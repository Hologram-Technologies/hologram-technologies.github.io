[General]
# theme.conf — the Hologram greeter theme. It reuses SDDM's real maldives Main.qml (MIT,
# installed verbatim alongside) so the genuine QML runs through Holo QML, but ships the
# Hologram wallpaper. `background` is the key the QML reads (Qt.resolvedUrl, relative to this
# theme dir). Authentication stays a self-sovereign key unlock bound to local hardware (Law L1).
background=background.jpg
