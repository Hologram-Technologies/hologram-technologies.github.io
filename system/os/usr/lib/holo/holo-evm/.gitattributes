# Vendored, content-addressed engine + EF consensus vectors (Law L5).
# Keep byte-exact on every platform (no CRLF conversion) so the sha256 κ pins
# in PROVENANCE.txt / bctests/index.json re-derive identically everywhere.
* -text
