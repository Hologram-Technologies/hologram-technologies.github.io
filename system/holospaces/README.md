# holospaces — the engine (submodule)

Consumed unmodified as a pinned submodule (ADR-006). Not materialized here to keep the project lean.

To build the OS, check it out:

    git submodule update --init holospaces

Pinned commit: bb742bb538fded40bcc698f97e6fb4c900fcb4ad
