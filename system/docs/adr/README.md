# Architecture Decision Records

Each ADR is one decision: its context, the decision, and its consequences. They are the
durable record of *why* Hologram OS is shaped the way it is. Naming follows the
[adr-tools](https://github.com/npryce/adr-tools) convention: `NNNN-title.md`.

The numbering is shared with the engine. **ADRs 0001–0021 are engine decisions** and live
upstream in the [holospaces](https://github.com/Hologram-Technologies/holospaces) submodule
(`holospaces/docs/`). **ADRs 0022+ are product decisions** and live here.

## Product ADRs

| ADR | Title | Status |
|---|---|---|
| [0022](0022-w3c-addressing.md) | Hologram-native addressing is W3C content addressing (`did:holo` · SRI · multihash · JSON-LD) | Accepted |
| [0023](0023-holo-theme.md) | Holo Theme — a W3C-native, browser-level design system | Accepted |
| [0024](0024-w3c-conformance.md) | W3C conformance is a declared, witnessed, enforced product gate | Accepted |
| [0025](0025-uor-object.md) | Everything is a self-verifying linked-data object (the UOR envelope) | Accepted |
| [0026](0026-sovereign-delivery.md) | The OS serves itself by hash — gateway-free, content-addressed delivery (one OS root κ · multi-source κ Service Worker) | Accepted |
| [0027](0027-survive-the-switch.md) | Survive the switch — total-offline boot (precache) and a signaling-free same-origin mesh (BroadcastChannel κ-exchange) | Accepted |
| [0028](0028-holo-ux-profile.md) | Holo UX Profile — system UX is one self-verifying, semantic-web object (φ proportion · voice · capability tiers · one κ every surface ingests) | Superseded by [0030](0030-holo-ui.md) |
| [0029](0029-adopt-the-desktop.md) | Adopt the desktop, don't run it — freedesktop/KDE standards as κ-objects, the DE as a swappable skin | Accepted |
| [0030](0030-holo-ui.md) | Holo UI — one UI subsystem, one control surface (window.HoloUI façade · holo-ui.html · whole UI as one holo://κ · staged mobile/WCAG/Lighthouse gates) | Accepted |
| [0031](0031-holo-conform.md) | Holo Conform — every spec is a self-verifying, byte-pinned UOR object under one κ-rooted index (import/discover/monitor; RFC 8785 the proof artifact) | Accepted |
| [0032](0032-holo-icons.md) | Holo Icons — open icon libraries (Material Symbols · Tabler) as κ-pinned, content-addressed objects rendered natively by `<holo-icon>`; makes `data-holo-icons` live | Accepted |
| [0033](0033-holo-constitution.md) | Holo Constitution — the constitutional core as an immutable, self-verifying, formally-consistent UOR object (8 principles as odrl:Rule κ-objects · exhaustive Z3-free proof · fail-closed conscience gate · governed succession) | Accepted |
| [0034](0034-holo-nanda.md) | Holo NANDA — agent discovery interop (Project NANDA) as a content-addressed projection: each holospace is one document that is a NANDA AgentFacts record ⊕ a self-verifying UOR object ⊕ a W3C VC (dual trust: signature today, content address when NANDA goes native); κ↔CIDv1 facts pointers; mint nothing | Accepted |
| [0035](0035-holo-skills.md) | Holo Skills — Agent Skills (agentskills.io) interop as a content-addressed projection: every capability is one `SKILL.md` the Hermes Agent skills feature consumes ⊕ a self-verifying UOR object (κ commits to the bytes), discoverable at `/.well-known/skills/index.json`; built from the MCP roster; mint nothing | Accepted |
| [0036](0036-holo-a2a.md) | Holo A2A — the Agent2Agent protocol as a content-addressed projection + a JSON-RPC bridge over MCP: each holospace is an A2A AgentCard ⊕ a self-verifying UOR object ⊕ an A2A-native detached EdDSA JWS (dual trust); `message/send` dispatches an MCP tool and returns an A2A Task (the fourth door over the one roster); contract pinned from the canonical proto; mint nothing | Accepted |
| [0037](0037-holo-resolve.md) | Holo Resolve — the universal resolver: any internet identifier/query (DOI · ISBN · Wikidata · place · CID · ENS · URL · free text) → a self-verifying UOR object over open web APIs, no AI; pure normalizers + byte-pinned provenance (Law L5) + cross-source reconciliation to Wikidata (the universal join) + the citation graph; the search bar as a content-addressed window onto the open web's object universe | Accepted |
| [0038](0038-holo-federate.md) | Holo Federate — federated unified search: one query fans across the open knowledge graphs in parallel and fuses into one self-verifying result set by Reciprocal Rank Fusion (cross-source agreement ranks; Wikidata Q-id reconciliation is the join); the whole search seals as a shareable, re-derivable holo://κ with per-source provenance leaves; no AI | Accepted |
| [0039](0039-holo-agenttrust.md) | Holo AgentTrust — verifiable, portable, tamper-evident agent reputation as a content-addressed, append-only, hash-linked chain: one κ commits to an agent's whole history (re-derive = verify across TIME); the UOR answer to NANDA's open trust problems (un-gameable telemetry, freshly-minted/BasisOS detection, real audit trails, portable reputation, irreversible revocation); closes the loop by making AgentFacts.evaluations.auditTrail a holo://κ that resolves to the chain; mint nothing | Accepted |
| [0040](0040-holo-answer.md) | Holo Answer — the composed answer card: a Google-style answer reconciled deterministically from the open web's self-verifying objects (no AI, cannot hallucinate); every fact is a schema:Claim linking to the source object it came from (a clickable did:holo bracket), facts agreed by ≥2 sources are corroborated and disagreements are flagged (deterministic contradiction detection); the whole card re-derives to the source bytes (Law L5, depth ≥ 3) and seals as a shareable holo://κ | Accepted |
| [0041](0041-holo-ask.md) | Holo Ask — the end-to-end query pipeline (the one search bar): one query → screen → route (certainty ladder: identifier ⇒ resolve, free text ⇒ federate) → top reconciled cluster → resolve sources → compose answer, sealed as one schema:SearchAction committing to the answer card AND the federated result set (the evidence) — one self-verifying address for a query, its answer, its evidence, and its provenance (Law L5, depth ≥ 4); no AI | Accepted |
| [0042](0042-holo-delegate.md) | Holo Delegate — verifiable, content-addressed capability chains (UCAN ⊕ UOR): agent A grants agent B a scoped, revocable authority that is one object — a valid UCAN delegation ⊕ a self-verifying UOR object ⊕ a W3C VC (signature bound to iss); the proof chain is a Merkle-DAG so escalation is caught by re-derivation (principal alignment + attenuation), a UCAN policy engine scopes invocation args, and revocation invalidates the whole subtree irreversibly; the authorization sub-layer of the agent stack; mint nothing | Accepted |
| [0043](0043-holo-atlas.md) | Holo Atlas — the community map of every holospace, native to the UOR substrate: it maps content truth, not popularity — each app by its did:holo identity, live integrity (re-derivation, Law L5), conformance specs, agent-door coverage (NANDA · A2A · Skills · MCP) and the shared-dependency closure graph (Law L3); built purely from the canonical indices by one isomorphic runtime (browser app ⊕ witness), sealed as one re-derivable atlas κ; self-updating, self-verifying, autonomous; mint nothing | Accepted |
| [0044](0044-holo-find.md) | Holo Find — the homepage: a Google-like search bar (`os/find.html`) that runs the unified-window pipeline (resolve → federate → answer) IN-TAB and live over the open web (Wikipedia · Wikidata · Crossref · Open Library · OpenStreetMap), no AI, no keys; paints the corroborated, source-bracketed, conflict-aware answer + the federated "see also", governed by the immune perimeter; verified live ("marie curie" → the person, a DOI → "Deep learning") | Accepted |
| [0045](0045-holo-orchestrate.md) | Holo Orchestrate — the verifiable multi-agent work receipt: a content-addressed execution DAG whose root κ is self-verifying proof of an entire agent collaboration; each step is a PROV-O activity linking, by κ, the agent's NANDA identity ⊕ AgentTrust reputation ⊕ the UCAN delegation it acted under ⊕ its inputs; re-derive the answer's κ (verifyDeep, Law L5) and you've verified who acted, how trustworthy, under what authority, and exactly what they produced — no orchestrator to trust; the keystone composing 034·039·042; mint nothing | Accepted |
| [0046](0046-holo-graph.md) | Holo Graph — browse the internet's object graph: a resolved object's edges (cited DOIs · the canonical Wikidata id) resolve to THEIR objects and nest, recursively, into one κ-rooted Merkle-DAG (the A27 self-authoring idiom — nesting = links, recursion = verifyDeep), re-derivable to source bytes (Law L5); the open web as one navigable, self-verifying graph, no AI | Accepted |
| [0048](0048-holo-settle.md) | Holo Settle — verifiable settlement: pay agents against PROVEN work, not claimed work (the trustless agent economy). A payer-signed schema:Order (x402-NP) commits to a work receipt + a split and releases a payment voucher per contributing agent ONLY if the receipt re-derives + every step was authorized + conscience-accepted (release ⇐ verifyDeep, Law L5); tampered work releases nothing; the voucher κ is the content-addressed txId (idempotent); the payee redeems trustlessly; the capstone composing 045·042·033; mint nothing | Accepted |
| [0049](0049-holo-agents.md) | Holo Agents — the agent stack made discoverable + usable by AI agents: the whole stack exposed as MCP tools (agent_facts · agent_reputation · verify_receipt · verify_delegation · verify_settlement · agent_passport) in tools/list + `/.well-known/mcp.json`, each returning a self-verifying result (verify, don't trust); ONE entry point at `/.well-known/agents.json` (itself self-verifying) lists every door (MCP · NANDA · A2A · Skills) + the verbs; wired via ctx.toolHandlers; mint nothing | Accepted |
| [0047](0047-holo-window-mcp.md) | Holo Window MCP — the unified window as governed, self-verifying MCP tools: `resolve_object` (extended to the open web), `search_web` and `answer` return self-verifying UOR objects an agent re-derives (Law L5), not prose it must trust; every tool passes the constitution's pre-dispatch gate (kill-switch supreme, immune-screened, audited); no AI in the resolution path | Accepted |
| [0051](0051-holo-forge.md) | Holo Forge — a build is a verifiable, re-derivable κ-transform: `κ(source) ⊕ κ(compiler) ⊕ κ(flags) → κ(artifact)`. A real, deterministic Holo-C → WebAssembly compiler runs serverless in the browser (and Node); the compiler is itself a content-addressed κ-object; every build mints a PROV-O receipt sealed to a `did:holo` that any peer re-derives byte-for-byte with no server (Law L5 extended from files to builds). Tamper is refused (changed source → changed κ; flipped wasm byte → validation fails). The receipt shape is transform-agnostic — the on-ramp to verifiable inference; unifies web2 (it runs) · web3 (trustless) · AI (verifiable transforms); mint nothing | Accepted |
| [0053](0053-holo-own.md) | Holo Own — verifiable, self-sovereign ownership on the content-addressed substrate (the "Own" era, no casino): a Title is a signed, content-addressed claim (owner `did:holo` ⊕ owned κ ⊕ prior Title; rights as ODRL; a W3C VC) whose head κ proves the whole ownership history (PROV-O); transfer is a UCAN-attenuated, signed, conscience-gated capability op (a non-owner or an escalation is refused — SEC-2/L5); scarcity is ANCHORED to an existing chain by reference (BTC/ETH/Solana via the chain kit) only when an asset needs it, never minted (Law L4); value settles against proven title (ADR-048); community/network ownership is a multi-principal Title; unites web2 (apps) · web3 (ownership + anchor + pay) · AI (agents as owners), composing 033·034·039·042·045·048 on the read/write substrate + `#kappa-parity`; mint nothing | Accepted |
| [0054](0054-holo-q-e8.md) | Holo Q · E₈ — every holospace-native LLM is compiled to a verifiable, deduplicated 2-bit κ-object against the ATLAS-declared E₈ quantization standard. The standard is a content-addressed UOR object (`did:holo:sha256:f2066c2d…`, lattice declared-by the atlas-12288 object — atlas → foldings → E₈); a model compiles once via incoherence (randomized Hadamard) → LDLQ adaptive rounding (per-layer input Hessian from a content-addressed calibration corpus) → E₈ lattice quantize → entropy-code → a κ-object referencing the standard, with a PROV-O compile receipt (source κ ⊕ standard κ ⊕ calibration κ → compiled κ); decode-on-load runs it on today's kernels (storage/dedup/verification win), native 2-bit kernels later (VRAM/speed). Validated: E₈ gain 0.86×, incoherence rescues 2-bit, LDLQ 5× lower output error, κ-object compile→decode→run in-browser. Honest scope: compression + verifiable model substrate — NOT interpretability / cross-LLM interop / no-hallucinations (tested, ruled out). Builds on 0052·0051; pins the design before the calibration-readback + assembly + 7B surgery | Proposed |
| [0055](0055-holo-build.md) | Holo Build — the third native holospace pillar (with Run · Share): a serverless, VS Code-faithful IDE running entirely in the browser (the real Monaco editor + xterm terminal, κ-verified) where every object's code is content-addressed, an identical build rebinds in O(1) (Holo Forge memo), editing re-renders the running app live (the visual Build), Quick Open + Command Palette make it familiar, and humans + AI agents co-edit in real time CONVERGING to one κ (collaboration proven by re-derivation, not trust). Honest non-goal: universal VS Code extension compatibility (needs a server) — replaced by the stronger, serverless path of WASM language servers + holo-native verifiable κ-extensions (skills/MCP). Composes 0051·0049·0050; mint nothing | Accepted |
| [0056](0056-holo-lang.md) | Holo Lang — language intelligence as a serverless, OFF-THREAD, content-addressed language service (the LSP pattern without a server): a Web Worker analyzer (shipped via `Function.toString()` + a Blob, no bundler) feeds Monaco's standard providers (document symbols/outline · find-references · rename · signature help · formatting) so analysis never blocks typing; the language server is a κ-object (the Holo Forge compiler for Holo-C, AND REAL production servers, vendored: **Biome** (Rust→WASM — lint + format for JS/TS/JSON) AND the full **TypeScript** language service (the real TS compiler in a κ-verified Web Worker — cross-file type inference + IntelliSense), both in-tab and serverless, composing in one Problems panel like VS Code's ESLint + TS; `clangd`/`rust-analyzer` + DAP next over the same transport) — the serverless, content-addressed substitute for a VS Code extension host. Composes 0055·0051; witnessed `#holo-lang` + `#holo-wasm-lsp` + `#holo-ts`; mint nothing | Accepted |
| [0052](0052-holo-q.md) | Holo Q — inference is a verifiable, re-derivable κ-transform: `κ(prompt) ⊕ κ(context) ⊕ κ(model) ⊕ κ(params) ⊕ κ(engine) → κ(output)`. A real LLM runs 100% in the browser on WebGPU (QVAC, vendored unmodified); the weights are a content-addressed κ-disk verified sector-by-sector off the substrate (no server sees your prompt); greedy decode is deterministic, so every answer mints a PROV-O inference receipt sealed to a `did:holo` that re-derives byte-for-byte (Law L5, the fulfillment of 0051's on-ramp). κ-memo gives O(1) reuse; conscience-bound (0033). Combines Forge's verifiable code provenance + hologram's O(1) content-addressed execution → the complete verifiable-AI stack | Accepted (app shipped; witness + gate row pending) |
| [0057](0057-holo-ui-conformance.md) | Holo UI — minimum text size (`--holo-font-min`, default 16px) + strict canonical conformance: the one engine drives the `--holo-*` tokens for every holospace and the readability floor lives in the token layer with `max()` so the cascade cannot bypass it; no first-party UI hardcodes a sub-floor px `font-size` (SDDM/Plymouth verbatim reproductions exempt); witnessed `#holo-ui-conformance` | Accepted |
| [0058](0058-holo-files.md) | Holo Files — the native, substrate-native file explorer (a files-community reproduction): one window onto the OS's content-addressed object universe + a writable OPFS Home; auto-detects the host OS and wears its native chrome (Finder · Explorer · Files · Nautilus), every object self-verifying by its `did:holo` κ (Law L5, a tampered byte refused), read-only over the immutable substrate · read/WRITE in Home; a core part of the World shell (dock + ⌘/Ctrl⇧E + a `holo-open` bridge); witnessed `#holo-files` | Accepted |
| [0062](0062-holo-ux.md) | Holo UX — the experience doctrine as one self-verifying UOR object (the experience twin of Holo UI's tokens): autodetect the host OS and adapt its native feel dynamically (modifier key · window-control side · font · accent — `holo-platform.js` → `data-holo-platform`, seeded, never overriding the user); thirteen tenets (the five founding principles + Steve Jobs's eight UX lessons), each a checkable obligation; capability tier · plain voice · a tier-aware resource budget (time/attention/compute/energy held sacred, interaction ≤100 ms / RAIL). Sealed as ONE κ-object (`etc/holo-ux/doctrine.uor.json`) that re-derives + whose source links re-derive (Law L5); the `hosux:` OWL/SKOS vocabulary mints nothing reusable. Sits under [0030](0030-holo-ui.md); revives [0028](0028-holo-ux-profile.md); witnessed `#holo-ux` | Accepted |
| [0060](0060-holo-link.md) | Holo Link — content-addressed composition: ship a linker, not a compiler. WASM is the universal IR already in every browser; the only gap was that `run(κ)` couldn't let a module name another. Now a module imports others BY CONTENT ADDRESS (the κ is the WASM import's module-name, so its bytes commit to its deps); running LINKS the graph from the store — verified (L5), instantiated once (L3) — so objects combine into new self-verifying objects and split back apart, connected only by one self-referential κ; a tampered dependency refuses the run. Build·Run·Share → Build·**Compose**·Run·Share over a universe of interoperable serverless apps. The lean answer to LLVM (no 100 MB toolchain). Composes 0051·0049·0055; witnessed `#holo-link` (closure, 9/9); mint nothing | Accepted |
| [0064](0064-holo-share-to-run.md) | Holo Share-to-Run — the growth loop: every shared link is a live, forkable app. The substrate's unfair asset is time-to-value ≈ 0 (no signup/install/server), so the native viral loop is **share-to-run-to-remix** (Loom ⊕ Figma ⊕ CodePen), not a central feed (forbidden by Law L1 / private-first). The rocket is built, the fuses unlit: teleport has the share machinery but the button was removed; `holospace.html` lands shared links in the World shell, not the running app; no Remix, no presence. Six binding rules (time-to-magic is the product · shared links carry `#k=` so they land fullscreen + a share-to-run chrome while internal `?app=` stays editable · contextual double-sided Share at six joy moments · one-tap Remix = a verifiable PROV-O/Title fork · "Made on Hologram" on every output · the loop compounds into presence/feed/follow by reference). Carries the **Phase 0 build spec** (~1 new file + ~10 lines of routing) + the `#share-to-run` witness it adds on acceptance. Composes teleport · holo-launch · apps/sdk · 0043 · 027 · 053 · 0057 · 0062; mint nothing | Phase 0 landed (witnessed `#share-to-run`) · rest Proposed |
| [0065](0065-holo-product.md) | Holo Product — the canonical upstream foundation for building beautiful, valuable and delightful Hologram-native products: it BUNDLES and BALANCES the two already-wired faculties of the UX·&·UI diagram — five UX (interaction design · wireframes · information architecture · user research · scenarios, from [0062](0062-holo-ux.md)) ⊕ five UI (visual design · colours · graphic design · layouts · typography, from [0030](0030-holo-ui.md)), each mapped to where it is wired + a checkable obligation, neither hemisphere shipping without the other — and binds a HYBRID delivery method (discover · define · design · build · verify · deliver) drawn from the field's PM frameworks (Agile · Scrum · Kanban · Waterfall · Lean · PRINCE2 · Six Sigma) that the substrate already lives (ADR = decision record · witness = definition of done · gate = stage-gate · budget = Lean · sealed κ = predictable artifact). Sealed as ONE κ-object (`etc/holo-product/product.uor.json`) that Merkle-links BOTH hemispheres + re-derives (Law L5); rests on the proven Holo UI + Holo UX rows; `hosprod:` mints nothing reusable; witnessed `#holo-product` (17 checks) | Accepted |
| [0069](0069-holo-route.md) | Holo Route — typed semantic streams, the routing plane: a pipeline is a content-addressed dataflow DAG of deterministic κ-transforms whose seams are type-checked by re-derivation BEFORE a byte flows (the pipeline commits to each seam type and refuses a mismatch, the way a Holo Link importer commits to an extern's type); each stage runs as a verifiable κ-transform (Forge/Q) accepted only after re-deriving its κ, memoized O(1) per `(stageκ⊕inputκ)` (Law L3), streamed with back-pressure for `list`/`stream` seams; the whole run seals as one self-verifying PROV-O κ any peer re-runs with no server (Law L5), and being an object it is shareable·ownable·settleable·delegable·conscience-gated. The third composition boundary — the *data* boundary — beside Holo Link's *call* boundary (0060) and Holo Orchestrate's *collaboration* boundary (0045); SDK verbs `pipe`/`route`; composes 0060·0051·0052·0045; mint nothing | Proposed |
| [0068](0068-holo-settle-plasma.md) | Holo Settle · the Plasma rail — verifiable settlement pays out in real, GASLESS USD₮ on Plasma (EVM L1, Chain ID 9745), released ONLY against work that re-derives (Law L5) and moved ONLY through Holo Wallet's existing default-deny, human-approval signing seam (`holo-wallet-bridge.js` + Tether WDK — no new crypto, NIHITO). Three additions, zero new primitives: (1) Plasma is a SEALED chain descriptor (`etc/holo-chains/plasma.uor.json`) — RPC/chainId/USD₮0-address content-addressed so a swapped param changes the κ and is REFUSED (anti-phishing is structural); (2) `redeemOnChain` reuses `redeem`'s re-derivation preconditions verbatim, then hands a USD₮ transfer intent to the wallet gate — tampered work ⇒ NO intent, no wallet ⇒ default-deny; (3) the on-chain dollar carries the voucher κ as a content-addressed memo, so the payment Merkle-commits to the exact proven-work receipt (auditable end-to-end) and keys an at-most-once guard. Entitlement is hash-based ⇒ POST-QUANTUM (independent of Plasma's classical finality — honestly scoped); gas abstraction is the unlock that makes sub-cent agent settlement real. Mirrors Holo Own's anchor-by-reference (Law L4); operator-opt-in, never a core dependency; composes 0048·0053·0033·0042; mint nothing but one sealed descriptor. Usable by humans (the Holo Wallet approval dialog) AND agents (a new `settle_onchain` MCP verb — re-derives first, returns proven gated intents, MOVES NOTHING); witnessed `#holo-settle-plasma` (7/7) + `agents-mcp-witness` now 7 verbs | Accepted |
| [0066](0066-holo-product-manager.md) | Holo Product Manager — the canonical full-cycle PM framework + pane, the bridge from idea to scalable product: STRICTLY adopts the Pragmatic Framework verbatim (37 boxes in 7 categories — Market · Focus · Business · Planning · Programs · Enablement · Support, strategic→tactical) and WIRES every activity Hologram realizes to the tool that executes it (User Personas → Holo UX · Use Scenarios → plain voice · Requirements/Roadmap/Positioning → Holo Product · Launch/Awareness/Advocacy → Share-to-Run · Pricing/Revenue → Own·Settle · Buy-Build-Partner/Innovation → Holo App · Measurement → re-derivation · Operations → conscience gate · Content/Sales Tools → Holo SDK), so it's executed not just described; the market/business boxes stay the PM's (NIHITO). Sealed as ONE κ-object (`etc/holo-pm/pm.uor.json`) that Merkle-links the Holo Product foundation it manages + every wired tool + re-derives (Law L5); canonical pane `holo-pm.html` + SDK `pm()`; `hospm:` mints nothing; witnessed `#holo-pm` (16 checks) | Accepted |

| [0070](0070-holo-trade.md) | Holo Trade — a self-sovereign trading terminal over **Hyperliquid** (on-chain order-book DEX, perps + spot): the read plane is the *already-shipping* HyperCore Info/WebSocket layer (`holo-hypercore.js`/`-stream.js`, reused verbatim — Holo Trade is its intent surface, Law L3); the write plane is the **official Hyperliquid SDK, vendored and SEALED** (`@nktkas/hyperliquid`/CCXT) so msgpack + phantom-agent EIP-712 signing is never hand-rolled (NIHITO, the docs' own rule). Five additions, zero new exchange code: (1) sealed SDK κ-object (tampered SDK ⇒ refused at load); (2) SEALED venue descriptor `etc/holo-chains/hyperliquid.uor.json` (endpoints + HyperEVM `eip155:999` + Arbitrum Bridge2 content-addressed ⇒ a swapped URL/bridge changes the κ and is refused — anti-phishing is structural); (3) every signature crosses the default-deny Wallet gate (`holo-wallet-bridge.js`, unmodified), and fast trading runs on a Hyperliquid **agent wallet** (`approveAgent`, withdrawal-less) wrapped as a **revocable, attenuated UCAN** (ADR-0042) — the tab's key can only trade, briefly, by permission you can withdraw; (4) every order passes the Constitution's pre-dispatch conscience gate (ADR-0033, risk limits) BEFORE signing, and every fill seals as a PROV-O `hostrade:Trade` receipt (ADR-0045) that re-derives to order ⊕ UCAN ⊕ verdict ⊕ on-chain tx; (5) the entitlement/audit is hash-based ⇒ POST-QUANTUM (Hyperliquid's secp256k1 action + USDC-bridge custody are classical — honestly scoped, not claimed). Builder codes are the transparent monetization; profit can pay out gasless via Settle-Plasma (ADR-0068). Composes 0033·0042·0045·0048·0053·0068 + Login/Wallet identity; mint nothing but `hostrade:`, one sealed descriptor, the sealed SDK; to be witnessed `#holo-trade` (+ `#holo-trade-render`) | Proposed |

| [0072](0072-holo-strategy.md) | Holo Strategy — the trading-strategy lifecycle (author → backtest → deploy) as content-addressed objects, by vendoring **QuantConnect LEAN** verbatim (Apache-2.0 — cleanly vendorable, unlike AGPL alts) as a SEALED engine κ; the strategy spec is LEAN's `QCAlgorithm` (Initialize/OnData/SetHoldings, C#/Python), no engine code written. Four additions: (1) sealed LEAN engine κ-object (tampered engine ⇒ refused before it runs, L5); (2) a strategy is a sealed `hostrat:Strategy` UOR object — its κ IS its identity (L1); (3) **the keystone** — a backtest is a RE-DERIVABLE κ-transform: LEAN's *determinism* makes the result a pure function of `engineκ⊕strategyκ⊕dataκ⊕configκ`, sealed as a `hostrat:Backtest` PROV-O receipt anyone recomputes to the same κ (L5, memoized O(1) L3) — you VERIFY a Sharpe, never trust a server's number (strictly better than QC's cloud); (4) live deploy routes every LEAN order through Holo Trade's default-deny wallet gate (ADR-0070) as a conscience-gated (ADR-0033), revocable UCAN agent (ADR-0042), every fill an Orchestrate receipt (ADR-0045), P&L via Settle (ADR-0048/0068) — NO new Hyperliquid brokerage (Holo Trade is the signer). Humans write QCAlgorithm; agents drive the identical lifecycle over MCP (`author/backtest/verify/deploy_strategy`, ADR-0049) → verifiable un-fakeable quant track records (ADR-0039). HONEST split on execution locus: WASM-native (.NET→wasm, the L1/L4 ideal, a real unfinished build — NOT claimed in-browser today) vs operator-opt-in Lean CLI/Docker (the interim, never core) — same re-derivable proof either way. Backtest history = Holo Trade's pinned Hyperliquid candles. Composes 0070·0051·0069·0045·0042·0033·0048·0049·0025; mint nothing but `hostrat:`, the sealed engine, one sealed data descriptor; to be witnessed `#holo-strategy` (+ `#holo-strategy-backtest`) | Proposed |

| [0071](0071-holo-bittensor.md) | Holo Bittensor — the live decentralized intelligence market (Bittensor / Subtensor) projected INTO the substrate's agent fabric as self-verifying UOR objects, NOT bridged as a foreign runtime (which would break L1/L4/L5): a Bittensor subnet IS already a NANDA registry (hotkey → neuron → axon → subnet task = AgentName → record → AgentFacts → endpoint → capability), so this generalizes Holo NANDA ([0034](0034-holo-nanda.md)) and is BETTER than a bridge — it re-derives. The metagraph at a block is a κ-rooted `dcat:Catalog` of neuron AgentFacts whose root `did:holo` PINS the block hash (re-derives against Bittensor's own Merkle chain, L5); each neuron is ONE document that is, on the same bytes, a NANDA AgentFacts record ⊕ a self-verifying UOR object ⊕ a hotkey-signed W3C VC ⊕ a `did:pkh` chain principal (composing the Chain Abstraction Layer); Yuma stake/consensus → a re-derivable AgentTrust attestation ([0039](0039-holo-agenttrust.md)) whose auditTrail IS the snapshot κ (un-gameable, capital-backed); a miner query seals a PROV-O inference receipt (Holo Q idiom, [0052](0052-holo-q.md)); receipts compose into a work receipt ([0045](0045-holo-orchestrate.md)) and settle TAO pay-for-proven ([0048](0048-holo-settle.md), testnet-gated — tampered work pays nothing). EVERY object is DUAL-AXIS substrate-anchored (`did:holo:sha256` ⊕ `did:holo:blake3` ≡ hologram's `kappa()`). Usable by agents (four `bittensor_*` MCP tools) AND humans (`holo-bittensor.html`, in-tab L5 re-derivation). HONEST: the Python `bt` SDK is not run (the chain read is an ingest boundary); a stochastic inference is receipt-verifiable, not reproducible; mainnet release is gated. Witnessed `#holo-bittensor` (27) + `#holo-bittensor-mcp` (15); mint nothing but `bt:` | Accepted |
| [0080](0080-holo-cosmos.md) | Holo Cosmos — real-time, navigable, infinitely-detailed environments streamed as κ-objects: the immersive render surface ("a cockpit exploring infinite space"). Records the correction that **AI super-resolution is NOT the path** (it hallucinates detail for a fixed 2-D image) and decomposes the objective into layers, each mapped to a part the OS already has: L0 verified κ-streaming (`holo-sources`/`holo-resolver`, ADR-026); L1 procedural infinite detail from a tiny κ **seed** (same seed → same universe everywhere, L5 — the honest source of "infinite"); L2 photoreal Gaussian-splat/NeRF places; L3 real-time present extending the zero-copy WebGPU layer (`holo-gpu.js`); L4 neural amplification in its CORRECT place — per-frame DLSS-style upscaling via Holo QVAC ([0067](0067-holo-qvac.md)) + the **already-in-tree** Real-ESRGAN ONNX/PROV-O pipeline (`tools/develop-sr.mjs`, `_nsr/`) for offline assets; L5 LOD streaming (O(view) not O(world)); L6 a `holo-space3d` surface every holospace mounts (camera = cockpit, scene = one κ). The living wallpaper (ADR-0061) is L3's Canvas2D rehearsal + the first scene. Honest costs: L1–L2/L4–L6 are heavy R&D, WebGPU-gated, SR detail is plausible-not-true. Composes 026·027·0061·0067; mint nothing; staged rollout, witness per stage `#holo-cosmos`/`#holo-space3d` | Proposed |
| [0076](0076-holo-heal.md) | Holo Heal — the OS heals itself, autonomously, by content: a content address is a lie-detector (Law L5 refuses a wrong byte) but not a healer — so `holo-heal` (`os/sbin/holo-heal.mjs`) resolves a missing/corrupt κ origin-agnostically (durable κ-store → origin → the open web via DID Core `alsoKnownAs` → IPFS, a sha-256 κ IS a CIDv1 → WebRTC peers), the FIRST copy that re-derives wins, every healed byte is seeded to the durable store (the device becomes its own redundancy + a peer source), and an idle-armed supervisor (`holo-heal-supervisor.mjs`, `requestIdleCallback`) re-verifies the whole closure, pre-heals latent corruption, appends a PROV-O receipt per repair, and seals a re-derivable health attestation (proves health, not asserts it); it EVOLVES — learns per-κ flakiness from each sweep's own integer history (no clock/randomness, re-derivation-safe) and re-sweeps unstable objects first. The one boundary is the constitution: the sealed anchor (`etc/constitution/*`, `holo-conscience.js`, the closure root) is excluded from healing (the loop can only restore correct content, never alter the law) and holds fail-closed while the law is broken. LANDED + witnessed (`holo-heal-witness.mjs`: recover·refuse·anchor-protected·seed·scrub·receipt·autonomous·evolve; `holo-web-witness.mjs`: open-web reach), `#self-heal` + `#open-web` required rows in `os/etc/conformance.jsonld`, browser-verified in Chromium; mints nothing | LANDED |
| [0077](0077-holo-suspend.md) | Holo Suspend — dehydrate ↔ rehydrate a live run to a content-addressed checkpoint: Holo Orchestrate ([0045](0045-holo-orchestrate.md)) proves a *finished* DAG; this adds the missing leg — stop a run in flight and resume it elsewhere. A paused run is ONE self-verifying UOR object (`hosus:Suspension`) whose κ commits to the program κ, the cursor, the FRONTIER (every live output, verbatim), the completed step chain, and the authority context κ. `rehydrate` `verifyDeep`s the frozen DAG (Law L5) before restoring — a tampered checkpoint or frozen step is refused, never resumed. Step receipts are timestamp-free, so a run suspended at step *k* and resumed reaches the SAME final receipt κ as an uninterrupted one (the pause is invisible in the proof), across machines (κ = content, not host); a model step's output is FROZEN in, so resume continues from it (never recomputes the past — correct for stochastic steps). The checkpoint is just a κ, so it migrates over the healer/resolver's torrent-style recovery ([0026](0026-sovereign-delivery.md)) and flows into Own·Settle·Orchestrate; resume cannot swap the plan or widen authority. Spiked + witnessed `holo-suspend-witness.mjs` (7/7, `Map` store + deterministic ops); promotion = `#suspend-resume` row + gate LIVE + durable `holo-kstore` + real A2A/MCP executor. Composes 0025·0045·0026·0051; mint nothing but `hosus:` | Proposed |
| [0081](0081-holo-mind.md) | Holo Mind — agency becomes an OS-wide, ambient, self-evolving fabric: the three agent doors ([0047](0047-holo-window-mcp.md)·[0049](0049-holo-agents.md)·[0035](0035-holo-skills.md)) compose into ONE verb surface a human OR agent draws from in every app + the shell, the canonical **intent→plan→act→verify→learn** loop lives as one OS service (anima's Nerve, re-expressed), and an app opts in by only conforming + declaring verbs. Two κ-transforms beside compile ([0051](0051-holo-forge.md)) and infer ([0052](0052-holo-q.md)): **ACTION** (`κ(intent)⊕κ(context)⊕κ(verbs)⊕κ(policy)→κ(action)`) passes the *existing* fail-closed conscience ([0033](0033-holo-constitution.md), `os/mcp/holo-mcp.mjs`) and seals a PROV-O work receipt ([0045](0045-holo-orchestrate.md)), payable ([0048](0048-holo-settle.md)) — human and agent seal the SAME receipt through the SAME gate; and **LEARNING** (`κ(traces)⊕κ(optimizer)→κ(improved-self)`) — the self-improving skill writeback [0035](0035-holo-skills.md) deferred — built on the adaptive-immune-classifier pattern ([0033](0033-holo-constitution.md), a learned model as a PURE FUNCTION of a content-addressed append-only corpus, re-derivable, Law L5) generalized from defense to all competence: a GEPA/DSPy reflective optimizer over an append-only trace corpus (API-only via `ask_model`, no training) mints an improved κ linking its parent `prov:wasRevisionOf` in force only after tests + size limit + conscience + operator ratification + cooling-off (governed succession, 0033 rule 4); letters-to-self are re-derivable self-assessment receipts — *you can prove how it learned*. The SOUL is one enforced objective: **maximize signal over noise by seeking coherence** (grow the user's verifiable κ-graph), drives PROPOSE goals the conscience must pass (self-discipline), the output court's judged principles (care·fairness·autonomy·responsibility·justice) are the measure of a good action (love·harmony·balance·life), a self-verifying user-model κ adapts + teaches (bound by Data Sovereignty/Holo Privacy), curiosity explores the open web ([0047](0047-holo-window-mcp.md)·[0034](0034-holo-nanda.md)·[0036](0036-holo-a2a.md)) but every fetched claim re-verifies before becoming signal. Holo Q = cognition · Holo Constitution = conscience · **Holo Mind = the self that acts, remembers, learns, becomes** — all re-derivable. Honest costs: the on-device model ceiling (ubiquitous ≠ smarter), the shallow 4-round loop, and above all the blast radius of OS-wide self-improving autonomy where the fail-closed conscience is the only barrier — Goodhart on the objective, a test-passing mis-alignment, curiosity as attack surface. Composes 0052·0033·0049·0047·0035·0036·0034·0042·0045·0048·0051; Phase 0 LANDED + PROMOTED — the isomorphic ambient-loop core (`os/usr/lib/holo/holo-mind.mjs`) + witness (`tools/holo-mind-witness.mjs`, 8/8 incl. byte-identity to the canonical envelope) + **required** `#holo-mind` row (in `gate.mjs` LIVE, ✓); SHELL-WIRED via `holo-mind-ui.js` → `window.HoloMind` (roster from the live MCP/skills doors · every step gated by `window.HoloConscience` · dispatch via the in-page `window.HoloMCP`), verified live in a real browser; REAL PLANNING wired (Holo Q's QVAC engine registers via `apps/q/ui/boot.js` → `setSampler`; the model's tool-calls become plan steps, verbs constrained to the roster, Law L4). **Phase 2 LANDED + PROMOTED** — `os/usr/lib/holo/holo-mind-evolve.mjs` (append-only trace corpus + governed self-evolution) + witness `#holo-mind-evolve` (10/10, **required**); the loop LEARNs (a Trace per run, a rewritten past Trace breaks every successor), `window.HoloMind.evolve` runs the optimizer under the ADR-033 succession gate, and an in-force revision RE-PROJECTS into the live roster (its SKILL.md frontmatter wins de-dup, ADR-0035) so an evolved skill ACTUALLY CHANGES BEHAVIOR — the corpus + learned skills DURABLE via the OS's own κ-store (`holo-store.js idbBackend`, Law L4), verified live (both survive a fresh page reload; an evolved skill leads the roster). The stochastic proposal stays non-reproducible (honest L5-over-the-audit-trail). **Phase 3 (the SOUL) LANDED + PROMOTED** — `os/usr/lib/holo/holo-mind-soul.mjs` + witness `#holo-mind-soul` (8/8, **required**): intrinsic DRIVES (epistemic hunger · fitness) that PROPOSE curiosity/self goals which run the ordinary conscience-gated loop (self-discipline is STRUCTURAL — a proposal is never an act; integer-only state re-derives), a DETERMINISTIC coherence (signal/noise) utility that can't be Goodharted by a self-grade beside the model-judged output court (Care·Fairness·Autonomy·Responsibility·Justice, ADR-033) at VERIFY, and private-first DURABLE user + self models (revisioned, divergence, never published — L1/Data Sovereignty) — verified live (drives move, `runProposals` runs each gated, models + drives SURVIVE a reload and re-derive). **Phase 4 (ORCHESTRATION) LANDED** — `os/usr/lib/holo/holo-mind-orchestrate.mjs` + `#holo-mind-orchestrate` (8/8, **required**): `orchestrate(intents)` runs N sub-agents in PARALLEL into ONE self-verifying PROV-O work receipt (Holo Orchestrate, ADR-0045 — `verifyDeep` re-runs each, a tampered sub breaks the root), EACH sub-agent acting under a **UCAN-scoped, REVOCABLE delegation** (ADR-0042 re-expressed: a `holo:Delegation` narrows its verbs ⊆ its granter's, attenuation chain content-addressed so escalation is caught; `revoke` kills the whole subtree; the work receipt commits to the delegation κ), plus scheduled tasks as re-derivable κ-objects fired by an opt-in in-tab ticker (clock at the edge, serverless) — verified live (3 parallel sub-agents → one re-deriving work receipt; a delegation narrows the 8-verb roster to 2, escalation refused, revoking a parent empties the child's scope; a scheduled task fires gated). Pending only: `os-closure.json` re-lock (maintainer build step); mint nothing but `holo:…/mind#` | Phase 0–4 landed + promoted (required `#holo-mind` · `-evolve` · `-soul` · `-orchestrate`) · complete |
| [0095](0095-holo-devtools.md) | Holo DevTools — the F12 developer environment, native to the κ-substrate and equally usable by humans, AI agents, and Holo Q. Every other holospace pillar is built (Build·Run·Share [0055](0055-holo-build.md), language intelligence [0056](0056-holo-lang.md), the scene model [0089](0089-holo-scene.md), the unified intelligence door [0091](0091-holo-q.md)) but there is NO way to INSPECT a running holospace the way F12 inspects a web page — the live object tree, resolved tokens, the κ-stream timeline, the heap of the object store are all opaque at runtime, for humans AND for an agent debugging an imported app ([0093](0093-holo-import-agent.md)) AND for Q. Two off-the-shelf options were named and are NOT the same kind of thing: Firefox Developer Edition is a whole BROWSER with a welded-in inspector (no embeddable/hostable frontend, no seam to repoint at a foreign backend) → **REJECTED**; `ChromeDevTools/devtools-frontend` is a pure CDP client over a swappable `Connection` with no assumption it talks to Chrome → **ADOPTED, vendored UNMODIFIED at a pinned commit** (Law L5: a tampered asset refused on read). The decision is forced, not a preference: the option that SEPARATES UI from protocol wins, because the substrate's value is the backend mapping and only Chrome's frontend lets us own that backend while inheriting a complete familiar UI for free. (1) Reject Firefox, adopt the Chrome frontend, write ZERO UI — only a small adapter + the backend. (2) A **κ-CDP backend** where every `nodeId`/`objectId`/`scriptId`/`requestId` is a content-derived alias for a κ (NOT a port of Chrome): **DOM**⇄the scene manifest tree ([0089](0089-holo-scene.md)/[0088](0088-holo-home.md), `backendNodeId`⇄κ, children=nested κ-objects); **CSS**⇄resolved `--holo-*` tokens ([0023](0023-holo-theme.md)/[0030](0030-holo-ui.md)) with the readability floor ([0057](0057-holo-ui-conformance.md)) as an un-overridable rule; **Sources**⇄the source κ (`publishSource`→`SoftwareSourceCode`, [0055](0055-holo-build.md)/[0056](0056-holo-lang.md)); **Network** = the DIFFERENTIATOR (not HTTP but the κ-addressed streamed-object timeline — per request: κ · axis sha256/blake3 · cache-hit (O(1) `Map.get`, Law L3) · L5-verify badge (`verifyDeep`, a tampered κ shows RED) · render cost · stream provenance `prov:wasDerivedFrom`); **Memory**⇄the κ store itself (heap node=κ-object, edges=`links[]`, dedup visible, [0060](0060-holo-link.md)). Reads are read-through-κ (`resolve`+`verifyDeep`); writes go through `handle.change`→`remix` child+`prov:wasDerivedFrom`. (3) THREE doors, ONE backend (mirror [0091](0091-holo-q.md)) — all call the SAME `HoloDevToolsServe`, the door changes only transport+governance not logic: HUMANS get the vendored frontend as a right-dockable/full-screen holospace; AI AGENTS get the same CDP methods as governed MCP tools (`devtools_inspect`·`_query_dom`·`_network`·`_eval`) + a CDP-over-REST seam at `/~holo-devtools/api` ([0093](0093-holo-import-agent.md)), each a self-verifying κ result, FAIL-CLOSED; HOLO Q gets a first-class verb, κ-memoized + deterministic. (4) Transport is in-process/cross-frame — the LOW-LATENCY choice: NO WebSocket, the frontend's `Connection` binds the EXISTING Q `holo-privacy:rpc`/`:res`/`:delta` bus (`holo-gov.js` `byWin`, host-asserted identity), CDP events ride `:delta`, the κ-memo IS the DevTools cache (O(1) repeat), stream deltas not whole trees; `holo-gov.js` gains ONE branch (`method:"cdp"`→`HoloDevToolsServe`). (5) Governance (Law L4): inspecting your OWN scene is sovereign (re-derivation, L5); beyond that fail-closed through the conscience ([0033](0033-holo-constitution.md)) — `Runtime.evaluate`+mutation route through `Q.agent` (governed/receipted, never raw eval, interior never handed out), cross-holospace needs a default-deny grant, remote rides the egress door ([0090](0090-holo-q-remote.md)) and opening a network transport is a separate unlit opt-in; every admitted op seals a receipt. HONEST: a CDP SUBSET not the whole protocol (unmapped → clean "unsupported", graceful degradation); NO live V8 stepping in phase 1 (Sources = source OBJECTS not a `Debugger` over running JS; Performance = substrate timings not a V8 profiler); `devtools-frontend` is a LARGE pinned bundle (built, not from-source); Network/Memory/Performance are RE-INTERPRETED onto substrate reality (the numbers mean substrate things, UI copy must say so); remote inspection gated+unlit; os-closure re-lock is the deferred branch-wide seal batch. STRICT CONFORMANCE (the brief's L1–L5 + 100%-W3C demand, named honestly): conforms to all five engine Laws by construction (L1 every handle is a content κ · L2 no new identifier scheme, only `did:holo`⊕BLAKE3 · L3 the κ-memo IS the cache, O(1) repeat = the "very fast" property · L4 mints nothing, no new authority · L5 `resolve`+`verifyDeep`, tampered κ refused). Two tensions named, not papered over: (T1) CDP is NOT a W3C standard and the vendored bundle is NOT a JSON-LD object → resolution: **CDP is the HUMAN door's PRIVATE transport, agents/Q NEVER speak it**; the bundle is a byte-pinned vendored artifact (κ+SRI) like Medusa/JupyterLite; the OPEN interoperable surface that must be 100% W3C is the AGENT/Q doors = MCP⊕OpenAPI 3.1⊕NANDA/A2A returning self-verifying JSON-LD/UOR/PROV-O (RFC 8785 JCS · DID+multihash · SRI · EARL 1.0, [0022](0022-w3c-addressing.md)/[0024](0024-w3c-conformance.md)/[0025](0025-uor-object.md)/[0031](0031-holo-conform.md)) — so the agent door is a FIRST-CLASS W3C door with its own contract+witness, NOT a CDP projection; (T2) WCAG/readability-floor ([0057](0057-holo-ui-conformance.md)) → a holo-theme token shim at the mount boundary else the verbatim-vendor exemption, declared in the witness. Composes 0091·0090·0093·0089·0088·0055·0056·0060·0023·0030·0057·0058·0033·0082·0022·0024·0025·0031; grounded in Laws L1/L2/L3/L4/L5; mints nothing. Vendoring recipe + the full CDP↔κ flow-map (§2 per-domain table, §7 Law-by-Law + spec-by-spec conformance map) + an 8-stage build plan (each stage one witnessable row) at [`../holo-devtools-cdp-flowmap.md`](../holo-devtools-cdp-flowmap.md). Witness PLAN owed on acceptance: `#holo-devtools-vendor`·`-cdp`·`-dom`·`-network`·`-runtime`·`-store`·`-doors`·`-conform` (the last backs the 100%-W3C claim) | Proposed (DESIGN ONLY — ADR + CDP↔κ flow-map + 8-stage plan; no app code, awaiting acceptance) |
| [0098](0098-q-fuse.md) | Q.fuse — a serverless COMPOUND model (the OpenRouter Fusion / pi-fusion "compound beats components" idea) brought onto the κ substrate as one more verb on the single door ([0091](0091-holo-q.md)): run a PANEL of small specialists ([0096](0096-holo-q-model-registry.md)) on the SAME prompt IN PARALLEL → a JUDGE compares them (consensus · contradictions · unique insights · blind spots) → a SYNTHESIZER writes ONE new answer none produced. The lift is the synthesis step and it is **model-size-AGNOSTIC** — a diverse panel of tiny on-device minds, judged + synthesized, beats any one of them — so the honest framing is **"compound beats components on the WASM floor", NOT "Fable-5 in a tab"**. NO new transport/gate/store (Law L4): every stage rides the existing κ-memo FABRIC ([0084](0084-holo-q-mux.md) `holo-q-fabric.js` — content-addressed compute over the durable OS κ-store), so an EXACT repeat fusion is **O(1)** (zero models run) and a NEAR-repeat (swap one panel member) re-runs ONLY the changed leg + judge + synthesis (the unchanged legs are cache hits) — where OpenRouter re-pays 5× every call and cannot reproduce a fusion, we pay once, address it, and seal ONE re-derivable `holoq:FusionReceipt` over the whole DAG (`prov:used` = every panel κ tagged by member + the judge κ; `prov:generated` = the synthesis κ; a tampered byte breaks the address, Law L5 — receipt canonicalization reused from [0090](0090-holo-q-remote.md) `holo-q-receipt.mjs`). The provider interface (`{id, generate?}`) means ANY generative provider is a panel member, INCLUDING a remote-broker-backed one ([0090](0090-holo-q-remote.md)): a member flagged `remote:true` is a governed frontier slot → the fusion seals `tier:"hybrid"` instead of `"local"`, a PER-CALL opt-in, never the default. Landed in `os/usr/lib/holo/q/holo-q-fuse.js` + wired as `Q.fuse`/`Q.configureFuse` (the host wires the panel, like binding a specialist; no panel → an honest notice, never throws). Node-witnessed 27/27 (`holo-q-fuse-witness.mjs`: parallel panel, distinct per-member κs, zero-model exact repeat, single-leg near-repeat, FusionReceipt re-derivation + tamper-refused, hybrid-tier flip). HONEST limits (stated): lifts above the best SMALL model, does NOT reach frontier; NO per-token streaming seal (κ seals at leg completion, streaming is transport); NO compute fan-out across the mesh ([0076](0076-holo-heal.md) recovers objects not compute — single-tab); suspend/resume of a long fusion ([0077](0077-holo-suspend.md)) is a spike, not promised. Default panel (host-wired, not pinned): Coder-1.5B (reason) + SmolLM2-360M (fast/diverse) + a third distinct FAMILY (diversity across families is what makes synthesis beat a vote); judge = Coder-1.5B; synth = the active brain. Composes 0091·0096·0084·0090·0033·0045·0081; mint nothing. Additive + reversible — remove the verb and the door is exactly 0091. Follow-ups (non-blocking): a `#q-fuse` conformance row, an OS-shell panel-config UI, the discovery loop choosing the default panel | Accepted (landed + Node-witnessed 27/27; conformance row + shell panel UI pending) |
| [0099](0099-holo-recall.md) | Holo Recall — serverless **κ-graph retrieval over the PRIVATE corpus** as one more verb on the single door ([0091](0091-holo-q.md)): `Q.recall`. The open-web stack ([0037](0037-holo-resolve.md)–[0046](0046-holo-graph.md), Resolve→Federate→Answer→Graph, no-AI, deterministic) answers *"what does the open web know"*; nothing answers *"what do **I** know"* over the user's own holospace objects · receipts · notes · κ-store. A four-sweep grounding (the [0098](0098-q-fuse.md) method) found ~85% already present and unconnected: VECTOR search = `holo-q-embed.js` ([0096](0096-holo-q-model-registry.md), bge-small on-device, content-addressed O(1) cache); RANK FUSION = `holo-federate.js` Reciprocal Rank Fusion ([0038](0038-holo-federate.md), pointed at the open web, never a local index); SYNTHESIS-WITH-GAPS = `Q.fuse` judge/synth ([0098](0098-q-fuse.md), consensus·contradictions·blind-spots IS a recall answer-with-citations-and-what's-missing); plus the durable κ-memo STORE/RECEIPT/GATE ([0084](0084-holo-q-mux.md)/[0082](0082-holo-prov.md)/[0033](0033-holo-constitution.md)). The motivating benchmark (a sweep of SovereignAI's *GBrain*): hybrid retrieval (vector ⊕ keyword ⊕ RRF) **over a typed knowledge graph built at WRITE-TIME with ZERO LLM calls** scored **+31 P@5 over vector-only RAG** — the graph is load-bearing and nearly free (edges by regex + heuristic link-typing, not a model), which on the κ substrate is the [0046](0046-holo-graph.md) nesting idiom applied to the PRIVATE corpus. Two genuinely NEW things (everything else is wiring): a **BM25 keyword index over the local corpus** + a **write-time auto-link typed κ-graph builder** (`holo-q-corpus.js`); the retriever (`holo-q-recall.js`) does HYBRID(vector⊕BM25)→RRF→1-hop typed-graph EXPAND→optional `Q.fuse` SYNTH (cited answer + gap analysis)→seal ONE `holoq:RecallReceipt`. Zero-LLM auto-link is the RIGHT call not a shortcut: a model-extracted graph is non-re-derivable (same input→different edges = L5 violation) + costs a call per write; regex+heuristic is DETERMINISTIC (same bytes→same edges, the index itself re-derives, dedups by content L3, grows on every write for free) — the read-time `synthesize:true` model step earns its keep on ambiguous entities instead. Bare `Q.recall(q)` = the fused graph-expanded chunk set (no model, deterministic, re-derivable); `{synthesize:true}` adds the answer card (evidence-first, the [0040](0040-holo-answer.md)/[0041](0041-holo-ask.md) shape). NO new transport/store/gate (Law L4); host wires the corpus via `Q.configureRecall` (no corpus → honest notice, never throws, like `Q.ask`/`Q.fuse`). STRICTLY separate from the open-web graph — provenance keeps the two universes apart, never silently merged. HONEST: PGLite (GBrain's WASM-Postgres) is NOT adopted — the κ-store IS the store (L4), we borrow the retrieval MODEL not the database; +31 P@5 is GBrain's, not yet reproduced on a Hologram corpus (a witness must measure OUR lift); auto-link trades recall on ambiguous entities for determinism+zero-cost. FOLD-INS recorded, deferred: (#1) a SOMATIC conscience axis (can the hardware sustain this — memory/VRAM/timeout pre-flight) is where the EXPAND fan-out + SYNTH call need a gate in an in-tab OS, the right home is the next `holo-conscience.js` touch ([0033](0033-holo-constitution.md)); (#2) ingestion via `lite-parse` (Rust→WASM PDF/DOCX→JSON, Tesseract-WASM OCR, in-browser) pinned as a Forge κ-transform ([0051](0051-holo-forge.md)) ONLY once a corpus is worth feeding. Composes 0091·0096·0038·0046·0098·0084·0082·0033·0040·0041·0081; mint nothing. Additive + reversible — remove the verb + two modules and the door is exactly 0098. Follow-ups (non-blocking): a `#holo-recall` conformance row + witness measuring OUR P@5 lift, shell wiring of `configureRecall`, the Omni bar surfacing private results behind open-web ones | Proposed (DESIGN ONLY; ADR drafted, no code, awaiting acceptance) |
| [0101](0101-holo-onnx-forge.md) | Holo ONNX Forge — make `hologram-ai` ("ari") 100% UOR-substrate-native and turn it into Hologram's native ONNX model compiler: **precompile any ONNX model once, then stream it as κ-addressable, demand-paged blocks** (deserialize-only, no JIT). THE LOAD-BEARING FINDING (from a first-principles map of the ari Rust workspace against the holospaces Laws): ari is NOT a foreign codebase to port onto UOR — its `Cargo.toml` already depends on the `Hologram-Technologies/hologram` substrate crates ("hologram 0.5.0 is UOR-native"), the SAME crates holospaces sits on, whose ADR-003 states it *"runs a tensor `.holo` via hologram's executor"*; **ari already emits the exact artifact the substrate executes.** So the compile→archive→execute CORE is already substrate-native (the `.holo` format, internal κ-addressing, per-value content-addressed elision are substrate-OWNED; ari delegates all hashing, never invents a parallel store; the model-file κ already re-derives via a TC-05 witness, L5; the hot path builds `default-features=false` with no reqwest/tokio/rayon/Python, proven by the live wasm build, L4). "100% native + streamable" is therefore NOT a rewrite but closing FOUR seams: **(A)** `.holo` persisted via `std::fs` → KappaStore put/get (breaks **L3** — the store is the address space, not a filesystem; ari-only); **(B)** weights are `path+offset` blobs (`AiParam::Mmap`, a misnomer — a whole `read_exact`; `memmap2` declared-but-UNUSED) + model κ is ONE coarse κ/model, off by default → per-tensor/per-shard κ-sectioning, `AiParam::Kappa{label}` not a path (breaks **L2**; needs UPSTREAM hologram-archive); **(C)** whole-file load (`std::fs::read`→`InferenceSession::load(&bytes)`, wasm verbs take the whole `&[u8]`, ~2× peak RAM) → a demand-paged loader where a page-fault IS a κ-resolve (breaks **L3** verbatim; needs UPSTREAM hologram-exec); **(D)** ingest = HF download (reqwest/tokio) + Python/PyTorch `torch.onnx.export` subprocess → declared-honest off-substrate pre-step that emits a κ-object (breaks **L1/L4**; the [0100](0100-holo-hermes.md) "incompatible-not-faked" precedent, L5). Seam B is the heart of "stream as κ-addressable objects" AND the riskiest assumption: ari treats the archive as opaque bytes, so B+C most likely require changes INSIDE the substrate crates (git deps, not vendored). Staging isolates that boundary: **Stage 0** `#holo-onnx-forge-store` (ari-only — `.holo` put/get by κ + OS2 SW κ-store byte-delivery, [0026](0026-sovereign-delivery.md), zero `std::fs` hot path); **Stage 1** `#holo-onnx-forge-blocks` (per-tensor κ at lower time, model root κ = E₈ product over block κ, every block re-derivable — minting half of B + D, no executor change); **Stage 2** `#holo-onnx-forge-stream` (THE upstream decision point — sectioned archive + demand-paged κ-resolve loader; a model larger than the resident budget loads, the 32-bit-tab ceiling lifts); **Stage 3** `#holo-onnx-forge-rederive` (retire whole-file load, verify-by-re-derivation PER BLOCK on the page-fault path, L5; PROV-O lineage [0082](0082-holo-prov.md) attached OUT-OF-BAND so it never perturbs the address). Disambiguation: "Forge" in [0051](0051-holo-forge.md) is the generic κ-transform/esbuild bundler; this is its model-archive sibling (the same "client bundler → κ-transform" pattern applied to weights). Honest limits: streaming (Stages 2–3) is the ONLY upstream-touching work — everything through Stage 1 is verifiable on ari alone; PyTorch conversion CANNOT be substrate-native (declared incompatible, never faked, L5); this fixes MEMORY + LOAD, not throughput (the wasm decode floor stays ~0.5–0.8 tok/s, single-threaded scalar — WebGPU/threads out of scope); holospaces enumerates no W3C/JSON-LD/PROV-O requirement (that is the OS2 product layer, [0022](0022-w3c-addressing.md), honored beside engine L1–L5). ari becomes the supply side of the model registry ([0096](0096-holo-q-model-registry.md)); Holo Q stays the live brain. Composes hologram-archive/-exec/-compiler · 0026·0096·0051·0082·0022·0100; grounded in Laws L1/L2/L3/L4/L5; mint only `holoq:ForgeReceipt` (out-of-band PROV-O). Spec at [`../specs/holo-onnx-forge.md`](../specs/holo-onnx-forge.md) | **Stages 0–1 LANDED + witnessed.** Stage 0 (ari `kstore.rs` 6/6 + OS2 browser half `holo-onnx-kstore.mjs` 16/16 incl. cross-substrate κ-parity on the real 352 MB `.holo` `blake3:767a2dd…`, browser-verified, SW serves `/.holo/blake3/<κ>`, `#holo-onnx-forge-store` row registered + gate PASS). Stage 1 (ari `manifest.rs` 7/7: `block_kappa` === the substrate `WeightFingerprint` lifted into ari's IR, per-tensor κ composed to an order-independent weight-set root, carried on `ModelMetadata.weight_root`, opt-in). Stage 2 LANDED + witnessed (upstream patch to `hologram-archive`+`-exec`: new optional `SectionKind::WeightIndex`; writer emits it in one pass; `LazyWeightStore` over a `RangeResolver` re-derives each fetched body L5; `exec/session` prefers the lazy path — one weight resident at a time, no all-bodies HashMap — with eager fallback; archive `weight_index` 3/3 + exec `weight_index_streaming` 3/3 compile→index→lazy-load→execute + full suites + real 352 MB model green; clippy+fmt clean; PR-ready `holo-onnx-forge-stage2-substrate.patch`, witnessed against a local clone). Stage 3 streaming transport LANDED + witnessed both sides (ari `stream.rs` `FileRangeResolver` pages weights from a `.holo` FILE by range, re-derived; SW `/.holo/blake3/<κ>` honors HTTP `Range`→206 + `holo-onnx-kstore.mjs` `makeRangeResolver` non-resident JS twin; witness 19/19; SW resealed 0 drift) — the one named remaining piece is the non-resident `load_streaming` executor path (archive head/weights split + structural fingerprint). Stages 0–2 closed |
| [0102](0102-holo-q-openrouter.md) | Q OpenRouter tier — frontier breadth as a GOVERNED, κ-MEMOIZED, receipt-sealed remote slot on the one door ([0091](0091-holo-q.md)), never an app-level fetch. OpenRouter is an OpenAI-WIRE aggregator, so it needs NO new subsystem: it rides the existing remote broker ([0090](0090-holo-q-remote.md)) as a DISTINCT `openrouter` adapter (not reused from `openai`, so the receipt names the engine honestly — the ollama precedent), the URL+key vaulted host-side (engine = a content address {wireFormat,modelId}, Law L1), egress the ONE brokered op (Law L4). The receipt PINS the routing truth (`holoq:servedModel`/`servedProvider`/`cost` inside the canonical body — an aggregator routes a slug to a real upstream, so the receipt names what ACTUALLY ran; a flipped served provider breaks the address, Law L5). THE MISSING WIRE (the magic): `makeRemoteProvider` wraps the broker as a κ-memo FABRIC provider ([0084](0084-holo-q-mux.md)), so an identical prompt is an O(1) hit with ZERO network calls — **pay-once** intelligence (Law L3); an injected durable `indexStore` makes pay-once survive a reload (closes the fabric v1 index gap). Surfaced as a `Q.fuse` HYBRID panel member ([0098](0098-q-fuse.md) `withRemoteMember`): the default panel stays 100% local, a remote member flips tier `local`→`hybrid` (per-call opt-in), and a near-repeat (swap a local member) leaves the remote leg a cache hit so the frontier model is NOT re-paid; an exact repeat runs zero models. The conscience gates EGRESS ([0033](0033-holo-constitution.md)): PII→P5 hard block, no grant⇒P4 block, the key never leaks. SOVEREIGNTY honest: NOT blind/ZK inference (FHE/ZKML/TEE is a future axis, pinned into the receipt only when a provider attests) — the real wins are local-stays-the-brain, egress minimization via the `holo-zk` selective-disclosure envelope (genuine SD-JWT membership proof), and progressive privacy via κ-memo (a repeat never re-egresses). Landed + Node-witnessed 26/26 (`holo-q-openrouter-witness.mjs`, mock fetch + REAL conscience: distinct adapter + served-meta + tamper-refused, P4/P5 blocks + key-no-leak, O(1) zero-fetch repeat, durable-index reload replay, hybrid-tier + near-repeat sparing remote + zero-model exact repeat, ZK envelope hides unrevealed); `#holo-q-openrouter` conformance row added + in gate LIVE. Composes 0090·0084·0098·0091·0033·0082 + holo-zk; mint only `holoq:InferenceReceipt` (served-field extended). Pending: the live shell installer hook behind an authed key, the os-closure κ-pin for the new served modules | Accepted (landed + Node-witnessed 26/26; conformance row green; live shell hook + os-closure pin deferred) |
| [0104](0104-holo-session-persistence.md) | Holo Session — a SIGNED-IN operator's ENTIRE experience persists on THE SAME MACHINE across reload + re-sign-in, rehydrating as continuity, not a feature. A first-principles look at the shell found the experience HALF-persisted and NOT user-scoped: widget boards · modes · wallpaper · voice prefs · the playground flag already write to `localStorage` (device-local, survives reload) but it is SHARED across operators, and the holospace STRUCTURE itself — the `tabs[]` (identity · title · κ-address · pinned/group · ORDER · the active tab) with each tab's `{world,layout,focusedId}` snapshot (the OPEN SURFACES) — lives only in memory and is lost on reload. Two facts shaped it: there is NO device identifier (identity is device-AGNOSTIC, [`holo-login.mjs`] — a seed unlockable anywhere — so the `(user,device)` keying needs a device-κ invented), and the local κ-store ([0026](0026-sovereign-delivery.md) `holo-kstore`, IndexedDB, content-addressed, L5-verifying) is exactly the right backing store, so "same machine only" = STAY in it + never fetch. THE MODEL: capture the whole experience as ONE PROV-O `holo:SessionManifest` (`holo:experience` = the lean tabs + activeTab + a `settings` map from an AUDITABLE localStorage allowlist `holo-widgets.*`·`holo.voice.*`·`holo:wall*`·`holo-vinyl.*`·`holo.q.*`·`holo.playground`), κ-SEALED into the local κ-store; κ = SHA-256 of its own canonical (RFC 8785 JCS) bytes, so an IDENTICAL experience seals to an IDENTICAL κ and `store.put` is idempotent (the κ-memo / O(1) property, here for free). Keyed to (operator-κ, device-κ): the operator κ is the signed-in session's operator (`signedInOperator()` — a GUEST or the device's fallback primary gets the clean default, which ALSO enforces per-operator isolation on a shared machine); the device κ is a random 16-byte κ minted once per browser profile (`holo.device.id`) that NEVER travels in a snapshot. Restore resolves the operator's head pointer → the κ-store → RE-DERIVES the bytes (L5 — a poisoned byte does not match its κ → clean default) → checks the manifest's device κ against THIS device (a store copied to another machine → clean default); `applyExperience` writes the saved settings back BEFORE the subsystems read them (the shell resolves the manifest before `initWall()`, the playground flag, and the deferred `holo-widgets`/`holo-voice` scripts run), so every subsystem rehydrates naturally in ONE deterministic reflow — no "restore previous session?" modal. Autosave is debounced on every meaningful mutation (tab open/close/switch/reorder/address · widget-board change · wallpaper + playground toggles) and a `visibilitychange→hidden`/`pagehide` flush captures EVERY axis on the way out — so SIGN-OUT (which navigates to the greeter) PRESERVES the snapshot, and the next sign-in as the same operator restores it. A quiet, signed-in-only desktop-menu escape hatch (Reset Saved Experience) forgets THIS device's snapshot. NOTHING leaves the device: the κ-store is local IndexedDB, NO fetch on any path (Law L4) — cross-device continuity is explicitly a DIFFERENT future axis (the `holo-zk` selective-disclosure egress envelope), NOT this row. HONEST boundary: tier capabilities held in the κ-store / conscience (not localStorage) are operator-agnostic + out of the allowlist — a restored capability is never faked (L5). Landed `os/usr/lib/holo/holo-session.mjs` (adapter-injectable core + browser bindings) + shell restore/autosave/flush/reset wiring; Node-witnessed 23/23 (`holo-session-witness.mjs`, in-memory adapters + a fetch spy: full round-trip of tabs+order+active+open-surfaces+every-settings-axis byte-for-byte · identical experience → identical κ + idempotent re-store · L5 tamper refused → clean default · per-operator isolation · same-machine-only device-binding → clean default under a foreign device κ · the reset escape hatch · the allowlist captures every axis + excludes identity/session/device keys · NO network egress); `#holo-session` conformance row added + in gate LIVE. Composes 0088·0089·0026·0084·0082 + `holo-login.mjs`; grounded in Laws L1/L4/L5; mint only `holo:SessionManifest`. Spec at [`../specs/holo-session-persistence.md`](../specs/holo-session-persistence.md). Pending: the os-closure κ-pin for the new served module | Accepted (landed + Node-witnessed 23/23; conformance row green; os-closure pin deferred) |
| [0106](0106-holo-session-sovereign-guest.md) | Holo Session v2 — raise [0104](0104-holo-session-persistence.md) to EVERY user + SOVEREIGN at rest. A Pass-1 audit found the load-bearing gaps: app-internal state (sandboxed iframes, no save protocol, per-origin storage SHARED across operators), the device-wide κ-store (never operator-scoped), GUEST work (not persisted), and NO at-rest confidentiality (plaintext JCS in IndexedDB). Crux tension: at-rest confidentiality vs a co-profile attacker needs a key only the operator can produce (their secret), but a key derivable at boot (silent reload-restore) is by definition also devtools-readable — they can't both hold in a browser. Decisions: at-rest = **Max sovereign** (encrypt under the sign-in secret; restore post-unlock); app coverage = **opt-in handshake**. THE MODEL: REALMS — `activeRealm()` is the operator κ when UNLOCKED, else a per-device GUEST realm; a guest's work autosaves to a device-key realm + restores at boot with ZERO action, and the instant they SIGN IN it is CLAIMED (re-keyed) under their vault key with NOTHING lost (write-new-then-delete-old; consumed head ⇒ ZERO bleed to another operator). AT-REST: the manifest is AES-GCM sealed BEFORE the κ-store (reusing the `holo-privacy.js` PBKDF2→AES-GCM pattern) with a SYNTHETIC IV (HMAC of the plaintext) so an identical experience still seals to an identical κ (κ-memo preserved) — operator key = PBKDF2(secret, salt=H(op‖device), 210k), never persisted; guest/locked = a device key (integrity + copied-store protection, honestly NOT devtools-secret). Restore: L5 re-derive → AES-GCM decrypt (wrong key/tamper → clean default) → device-binding. HARDENED: a per-realm `seq` guard kept OUT of the body (a stale 2nd tab can't clobber; ADR-0105 dedup untouched), `holo:v` + a v1→v2 migration (a v1 plaintext manifest restores forward), quota-graceful saves (never corrupt the last good snapshot). APP STATE: a tiny opt-in `holo-session:*` postMessage handshake (`holo-session-client.mjs`, one import) folds a participating app's state into its world node so it rides the encrypted manifest; non-adopters keep per-origin storage (honest documented gap). Wired into the greeter `go()` via `window.HoloSession.onSignIn(operator, secret)`; every-user autosave + guest boot restore + visibility-gated saves. NOTHING leaves the device (no fetch any path, Law L4); cross-device stays ADR-0105 (a PLAINTEXT export, orthogonal). Node-witnessed 33/33 (`holo-session-witness.mjs`, in-memory adapters + a REAL AES-GCM cipher + a fetch spy: encrypted round-trip of every axis incl. per-app state · ciphertext-at-rest + wrong-key/tamper → clean default · κ-memo determinism · guest→claim ZERO loss + consumed head + ZERO bleed + claimed-data-not-device-readable · per-operator + same-machine-only isolation · seq guard · v1 migration · quota-graceful · NO egress); `#holo-session` row updated to v2, gate LIVE. HONEST: a bare reload can't silently restore an operator (Max-sovereign trade — restore is post-unlock); non-adopting apps aren't operator-scoped; the device key isn't devtools-secret. Composes 0104·0105·0026·0084 + `holo-login.mjs` + `holo-privacy.js`; Laws L1/L4/L5; mint only `holo:SessionManifest` (now encrypted at rest). Pending: os-closure κ-pin for the new modules; whole-catalog handshake adoption | Accepted (landed + Node-witnessed 33/33; conformance row green; os-closure pin + per-app adoption deferred) |
| [0094](0094-holo-hub.md) | Holo Hub — a Shopify-class commerce experience for the universe of κ-addressed holo apps, **100% serverless · κ-anchored · in-tab · fast in any browser**, where a **PRODUCT IS a holo app addressed by its `did:holo` κ**, built by COMPOSING the substrate's already-shipped commerce primitives and **ADOPTING Medusa's commerce MODEL as a byte-pinned spec ([0029](0029-adopt-the-desktop.md) "adopt the desktop, don't run it") — NOT running Medusa's server**. THREE findings drive it. FIRST — the substrate already SHIPS most of "commerce", so Hub is a COMPOSITION not a platform: product=app=κ (the `apps/index.jsonld` catalog, 41 apps, IS a product feed); RENDER from κ = `publishSource` ([0088](0088-holo-home.md)/[0092](0092-holo-import.md)); INFINITE NESTING = Scene+Graph+Link ([0089](0089-holo-scene.md)/[0046](0046-holo-graph.md)/[0060](0060-holo-link.md), a Hub nests in a Hub); DISCOVERY = Atlas ([0043](0043-holo-atlas.md))+Federate/Ask+NANDA; OWNERSHIP = Holo Own Title ([0053](0053-holo-own.md)); MONETISATION = Settle+Plasma gasless USD₮ ([0048](0048-holo-settle.md)/[0068](0068-holo-settle-plasma.md))+HTTP-402; ANALYTICS = PROV-O/Holo Prov ([0082](0082-holo-prov.md)). The genuine GAP — collections/variants/prices/regions/tax/promotions/inventory/cart/checkout — is exactly the model Medusa is the proven source of truth for. SECOND — "run Medusa verbatim" and "100% serverless in any browser" are MUTUALLY EXCLUSIVE, evidence decisive: Medusa v2's modules run in any **Node** env incl. serverless-**Node** runtimes over **MikroORM+PostgreSQL** (SQLite non-core, **no browser/WASM path**) — its "serverless" = serverless-Node-with-a-DB, NOT in-browser; running its packages in a tab needs Node polyfills + SQL-in-WASM + MikroORM-in-browser = unsupported + slow, refused by L4/[0029](0029-adopt-the-desktop.md)/[0092](0092-holo-import.md) (backend/DB→`out-of-scope-v1`). The operator made **100% serverless NON-NEGOTIABLE**, so the prior off-device-egress draft is SUPERSEDED — the server is ELIMINATED, not relocated. THIRD — the OS's own deepest pattern IS the resolution: **ADOPT, DON'T RUN** ([0029](0029-adopt-the-desktop.md) freedesktop-as-κ, [0031](0031-holo-conform.md) spec-as-byte-pinned-κ, [0034](0034-holo-nanda.md)/[0035](0035-holo-skills.md)/[0036](0036-holo-a2a.md)). **THE HONEST TRADE, NAMED (pushed back):** RELAXES the brief's "run Medusa verbatim / zero new code" to "ADOPT Medusa's model + vendor-and-compile its PURE functions where they exist" — the two original constraints can't both hold (Medusa is a Node+DB server); serverless is the one kept. THE MODEL = three planes, ALL serverless+κ+in-tab: (1) TRUTH (product=app=κ, feed=`apps/index.jsonld`); (2) COMMERCE-MODEL (Medusa's data model + Store-API OpenAPI **byte-pinned as a κ-object** = source of truth + drop-in Store-API shape; its PURE calc fns — price-list/tax/promotion/cart-totals — **vendored + Forge-compiled to verifiable WASM κ-transforms** [[0051](0051-holo-forge.md)], re-derivable L5; DI/ORM/HTTP glue realized NATIVELY as κ-transforms, the [0029](0029-adopt-the-desktop.md) trade); (3) ENGINE+EXPERIENCE (the Hub holospace, sealed κ, twin of Atlas — **persistence = the κ-store** [`holo-store.js`, no Postgres/Node/egress], renders product=app cards from κ with live verify, one-tap Open renders the live app via Scene, cart→checkout computed IN-TAB by the adopted-Medusa κ-transforms, BUY=a Title over the product κ + Wallet/Plasma payment, PUBLISH=`publishSource`→a product κ-object; only network is governed κ-asset-streaming [[0026](0026-sovereign-delivery.md)], core loop 0-network). Includes a Shopify→Hologram map (storefront→Hub SPA+Scene · catalog→κ feed · cart/checkout→in-tab κ-transforms · payments→Wallet/Plasma/402 · variants/pricing/tax/promo→Medusa model adopted+Forge-compiled [the one place new/compiled logic lives] · install→Own Title · discovery→Atlas/NANDA · publishing→publishSource→product κ · analytics→PROV-O · App-Store→recursive holospaces · agent commerce→pinned Store-API shape as MCP/`/~hub/api`). HONEST: relaxes verbatim→adopt (named, the 0029 pattern); fidelity is to the MODEL+API contract (byte-pinned, re-derivable), not Medusa's process; the κ-store IS the database (relational queries→κ-graph indexes, durable via `holo-store.js`+[0076](0076-holo-heal.md)); the extractable-vs-native split is resolved Stage 1, surfaced never faked; listed apps UNTRUSTED; payment finality rests on wallet+chain-anchor ([0068](0068-holo-settle-plasma.md)). Composes 0043·0053·0048·0068·0089·0046·0060·0051·0092·0093·0031·0029·0076·0082·0034·0033; grounded in Laws L1/L3/L4(0-network core)/L5; mint nothing. Feasibility note + staged plan at [`../holo-hub-exploration.md`](../holo-hub-exploration.md). Witness PLAN owed on acceptance: `#holo-hub` + `holo-hub-witness.mjs` (Medusa model/OpenAPI pinned as a re-deriving κ · catalog→product set with κ identity · a Forge-compiled pricing/tax κ-transform re-derives byte-for-byte L5 · product κ renders+re-derives · store-in-store round-trips one composed κ · cart→checkout 0-network · buy→Title+re-derivable settle receipt · tamper/forged-Title refused · baseline grants no capability) | Proposed (DESIGN ONLY, REVISED for 100%-serverless — supersedes the off-device draft; ADR + feasibility + staged plan; no app code, awaiting acceptance) |
| [0093](0093-holo-import-agent.md) | Holo Import → agent-native — the moment a GitHub repo imports ([0092](0092-holo-import.md)) it is auto-WIRED into the MCP + /~<app>/api REST seams every holospace ALREADY exposes, so any AI agent or holo app can fetch · re-derive (L5) · run · share · discover it with ZERO author effort: Railway's "connect a repo → it runs" feel, zero servers, agent-native by default. THE FINDING that makes it small (not a platform): the endpoints ALREADY exist — every holospace has an MCP server generated from `apps/<id>/holospace.json` (`holo-mcp.mjs`, every resource a self-verifying UOR object + a standardized capability card `holo://capabilities`) AND a unified REST surface at `/~<app>/api` (`holo-api-core.mjs`, content-addressed κ ingress/egress, HTTP-402-payable, **mounted on every tier incl. the Service Worker** → serverless by construction); both take the SAME `resolve(uri)→self-verifying object` over one κ-store, and an imported app's source IS already such an object (the publishSource envelope verifies under `holo-object.verify`). So agent-native = DERIVING the one manifest + one resolver that mount the app into seams that already work. THE MODEL: (1) BASELINE (every imported app, free) — `deriveManifest` emits a holospace.json declaring the app source as a self-verifying RESOURCE (uri = app κ); `deriveAgentSurface` returns manifest + `resolve` + a NANDA AgentFacts record; fed to `buildAppRegistry`+`makeApiServer`, an agent can instantly LIST (tools/list·resources/list, app-scoped server), FETCH+RE-DERIVE (resources/read · GET /o/<κ> with x-holo-verify, L5), RUN/SHARE/INSPECT (universal built-ins resolve_object·verify_object·holo_describe·holo_run·holo_share over the app κ), and be DISCOVERED (AgentFacts) — author writes nothing; (2) ONE resolver, two surfaces, no parallel infra (`ctx.resolve === api.opts.resolve`, isomorphic → runs in the SW, 0-network); (3) **EXPOSURE ≠ AUTHORIZATION** — the manifest exposes, grants NO capability; baseline tools:[] (the verbs are universal read-only/forge built-ins, not app privileges), effectful capability default-deny behind the conscience ([0033](0033-holo-constitution.md)) → auto-mounting an untrusted repo hands out nothing but verify-able content; (4) DECLARED surface LANDED for OpenAPI JSON + typed exports — `detectDeclaredSurface(files)` projects an OpenAPI/Swagger spec into ONE faithfully-schema'd MCP tool per operation (query/path params + requestBody → correct inputSchema) and typed exports into descriptive tools; they land in tools/list + the capability card so an agent DISCOVERS the app's real API, each carrying an honest `x-holo-exec` (egress=needs a governed grant, NOT serverless; sandbox=staged) — `execDeclaredHandler` runs neither, so calling a projected tool returns an honest error: discovery, NEVER authorization (OpenAPI-YAML/postMessage/actual execution staged). INCLUDES a Railway→Hologram architecture map (connect-repo→classify/build-locus · service→κ-app · env/secrets→config κ/vault · managed-DB→κ-store [the honest serverless gap] · private-networking→MCP+REST self-verifying·governed·discoverable · logs→PROV-O receipts you re-derive · templates→shared κ+remix · canvas/CLI/API→scene graph+Q.agent+/~api · pricing→free 0-network core, 402-meter egress). Landed `os/usr/lib/holo/holo-import-agent.mjs` (pure, isomorphic, deps injected); Node-witnessed 45/45 (`holo-import-agent-witness.mjs`, against the REAL holo-mcp + holo-mcp-core + holo-api-core + holo-object: app-scoped MCP server · app κ in resources/list · verbs exposed · resources/read + REST GET /o/<κ> re-derive L5 · verify/resolve/describe over the app · capability card self-verifies + lists the app · ONE resolver serves MCP+REST · tamper→verify false + REST 404 · AgentFacts self-verifies + endpoints + lineage to app κ · baseline grants no capability · deterministic/O(1) · LIVE createLiveRegistry registers an in-memory imported app → in-page MCP + REST answer with no reload · DECLARED OpenAPI→3 faithful tools (typed params + required body) in tools/list + the card, egress-op call → honest error, AgentFacts advertises them, no baseline regression, typed exports→sandbox tools); existing holo-app-mcp + holo-api witnesses still pass. LIVE AUTO-REGISTRATION LANDED — the shell (`shell.html`) on import calls `registerLiveAgent(li)` → `window.HoloImports` (a `createLiveRegistry` over the node-free MCP engine + isomorphic REST core), so an imported app answers MCP + /~<id>/api with NO reload/NO server; a Buffer-free `holo-object.address()` (byte-identical) makes verify/L5 run in the browser+SW. VERIFIED LIVE in the booted shell (localhost:8123): a real import of github.com/octocat/Spoon-Knife answers MCP (lists/resolves the app κ, object re-derives in-browser/L5, verbs exposed) + REST (GET /~io.github.octocat.spoon-knife/api/o/<κ> → 200 + x-holo-verify:true), no reload. SERVERLESS TRANSPORT BRIDGE LANDED + browser-verified — the SW (`holo-fhs-sw.js`) already serves /~<id>/mcp + /~<id>/api from a manifest+κ; the page writes an import's manifest + self-verifying κ-objects into the `holo-imports-v1` cache (`swCacheEntries`+`publishImportToSW`), the SW's `mcpManifest`/`mcpResolve` read it FIRST, so an EXTERNAL HTTP agent reaches an in-memory import (GET .well-known/mcp.json→200 · POST /mcp tools/list→app+built-ins · GET /api/o/<κ>→200+x-holo-verify:true) with NO origin server. FORGE BUNDLER CLASS LANDED + browser-verified — `holo-forge-bundle.mjs`: a client-bundle repo (Vite/React-shaped) → ONE self-contained app via a real esbuild κ-transform (esbuild INJECTED — Node esbuild / browser esbuild-wasm; relative imports from the repo, bare deps via resolveBare=the CDN ingest boundary; deterministic→re-derives, κ-parity, forgeReceipt); `importRepo` gains an injected `bundle`, the forge class runs only with a bundler else the honest notice; Node-witnessed 12/12 with REAL esbuild + live (esbuild-wasm@0.21.5 from CDN, a Vite-shaped repo→forge→runnable+self-contained+sealed κ). HONEST: "fully agent-accessible" holds for baseline κ-verbs + DECLARED interfaces, NOT an arbitrary app's DOM (sandbox fallback SURFACED); managed-DB is where serverless ends; Forge covers esbuild-bundleable repos (plain JS/TS/JSX + CDN deps), NOT framework-build-plugin repos (Vite/webpack env/asset/SSR) — true "any repo" is deeper. VENDORED κ-TOOLCHAIN LANDED — `tools/vendor-esbuild-wasm.mjs` vendors esbuild-wasm on-disk, sha256-pinned (`esbuild-wasm.pin.json` committed; the ~11.7MB binary gitignored like the Voice weights); the shell loader is LOCAL-FIRST + κ-VERIFIED (re-derives esbuild.wasm against the pin before trusting it, L5; CDN only as fallback) — removes the build-tool CDN dependency; witnessed 6/6 + live (loads same-origin, κ matches pin, bundles with NO CDN). GOVERNED INGEST LANDED (Law L4) — `makeGovernedFetch` gates EVERY import fetch (GitHub bytes, CDN dep) through the conscience BEFORE it happens: a red-line P5 PII URL / ungranted host is REFUSED (network never touched), accepted → a re-derivable `hosc:Ingest` receipt; fail-closed; shell routes ghFetchRepo + dep resolver through it vs `window.HoloConscience`; witnessed in holo-import-witness 38/38 (REAL scanPii) + live (PII blocked, clean allowed). Composes 0092·0051·holo-mcp·holo-api·0034·0047·0082·0090·0033; grounded in Laws L1/L3/L4/L5; mint only `nanda:AgentFacts`. GOVERNED-EGRESS EXECUTION LANDED — `makeEgressToolHandlers` makes a declared OpenAPI tool CALLABLE but DEFAULT-DENY per host (via makeGovernedFetch): a granted call fills path/query/body + seals a re-derivable hosc:Egress receipt, ungranted → refused (network untouched), unreachable backend → honest error; createLiveRegistry({governedFetch}) wires it as MCP toolHandlers; the shell gates on `window.HoloImportGrants` (empty by default — exposure ≠ authorization) + the real PII red-line; witnessed 53/53 + live (a `zen` tool default-denies then executes api.github.com/zen after grant + receipt). SEALED — two conformance rows added to `os/etc/conformance.jsonld` (`#holo-import`→holo-import-witness, `#holo-import-agent`→holo-import-agent-witness, required); `npm run gate` PASSES (both rows witnessed green in os/etc/earl-report.jsonld); the drifted shell.html closure pin resealed (`tools/reseal-drift.mjs`). Composes 0092·0051·holo-mcp·holo-api·0034·0047·0082·0090·0033; grounded in Laws L1/L3/L4/L5; mint only `nanda:AgentFacts`. Pending: OpenAPI-YAML/postMessage projection, the sandbox-exec path, pinning the NEW modules into the os-closure (the cross-repo image build — branch-wide batch) | Proposed (agent-surface + baseline + LIVE auto-registration + DECLARED tier + SERVERLESS TRANSPORT BRIDGE + FORGE BUNDLER CLASS + VENDORED κ-toolchain + GOVERNED INGEST + GOVERNED-EGRESS EXECUTION landed + Node-witnessed 53/53 agent · 38/38 import · 12/12 forge · 6/6 vendor against real cores/esbuild/conscience + verified live with real repos/esbuild-wasm/PII+grant gates + `#holo-import`/`#holo-import-agent` conformance rows GREEN at the gate + drift resealed; YAML/postMessage + sandbox exec + new-module closure-pinning staged) |
| [0092](0092-holo-import.md) | Holo Import — paste a GitHub URL, get a running, content-addressed Holo app: import is a DETERMINISTIC ENCODER onto the existing publishSource/Q spine, NOT a new runtime, and it is HONEST about the line where "serverless" ends. The ask (Create mode: paste a repo URL → Hologram ingests + encodes + runs it as a first-class Holo app, 100% serverless, in-browser, Railway-seamless but the deploy target is the DEVICE + the κ-store) forced two facts apart. FIRST, **not every repo is a browser-runnable serverless artifact** — a static site/client-SPA is, a Node/Python/Postgres backend is not (no server on a serverless device), so the load-bearing question is the **BUILD LOCUS**, decided per class exactly as [0051](0051-holo-forge.md)/[0072](0072-holo-strategy.md) faced it; faking a runnable app would be the dishonesty this OS refuses. SECOND — the finding that makes it SMALL — a Holo app is already nothing but a self-contained HTML source sealed by `repo.publishSource` into a `schema:WebApplication` κ ([0088](0088-holo-home.md)), so *once a repo is reduced to ONE self-contained HTML string it seals through the IDENTICAL envelope a studio-built app does — byte-for-byte the same κ*. Import is therefore NOT a new object/runtime but a DETERMINISTIC ENCODER producing a source the substrate already renders·links·remixes·shares·traces. THE MODEL: (1) `classify({files,pkg})` reads package.json + the file listing (never guesses) → static/SPA→`direct` (runs v1), client-bundle→`forge` (in-browser bundle, [0051](0051-holo-forge.md), STAGED), SSR→`translate-or-forge` (staged), backend/DB/non-JS/CLI/library→`out-of-scope-v1` with the reason SURFACED (`runnable:false`, nothing faked); (2) `encodeStatic` inlines relative same-origin `<link rel=stylesheet>`/`<script src>` VERBATIM (byte-preserved, no model) into one self-contained HTML, while ABSOLUTE/cross-origin/API refs are recorded in `externalRefs` + `selfContained:false` (the "log what we don't run" rule, structural); (3) `sealApp` builds the EXACT publishSource UOR envelope (same @context/@type/JCS/SHA-256) so **the imported app κ EQUALS a studio app κ for identical bytes** (first-class, not a wrapper — proven against the real HoloRepo), and the app drops into any holospace as ONE κ-object ([0089](0089-holo-scene.md)); (4) a new DETERMINISTIC mux task `import` (`deterministic:true` → discovery SKIPS it, no HF model, never pretends to pick one) routes `Q.create(repoUrl,{task:"import"})` onto the κ-memo fabric ([0085](0085-holo-q-fabric.md)) — a repeat is O(1) `cached:"hot"` and the encoder isn't re-run, so **the ingest happens ONCE, a re-import re-fetches NOTHING** (L3, and the precise honoring of L4: the network is an INJECTED boundary, hit once); (5) `importReceipt` (PROV-O) pins `prov:used = git+repo@commit` (the resolved SHA — a branch is mutable) → `prov:generated = app κ`, records the build-locus class, and names every `hosc:unservedExternalRefs`, so a remote import carries honest lineage + an honest coverage statement in Holo Prov ([0082](0082-holo-prov.md)). Landed `os/usr/lib/holo/holo-import.mjs` (pure, isomorphic, zero-dep, deps injected) + one `holo-q-mux.js` task + wired into the shell Create studio (`shell.html` `openCreate`); Node-witnessed 32/32 (`holo-import-witness.mjs`, MOCK fetch — Law L4 — against the REAL holo-blocks-repo + holo-scene + Q/trinity/mux: parse incl. bare github.com · classify the four classes truthfully · byte-faithful inline · external refs surfaced · **imported app κ === publishSource κ** · re-import re-derives same κ · tamper→different κ refused (L5) · receipt pins git+commit + names unserved refs · scene round-trips same κ · **Q.create({task:import}) O(1) hot + re-fetches nothing** · backend/bundle → runnable:false with the reason); the existing Q witnesses (unified 17/17, spine, q) still pass after the mux edit. VERIFIED LIVE in the booted shell: the module is isomorphic in-browser (**κ-parity holds in the real browser**), `openCreate` binds the importer (routeTask("import")→holo-import), and `window.Q.create("github.com/octocat/Spoon-Knife",{task:"import"})` ingests the REAL repo (commit pinned) → faithful HTML → real app κ, repeat O(1) `cached:"hot"` ~0.06 ms same κ. HONEST: only the static/SPA class runs in v1 (a real large slice, NOT "any repo"); the bundler `forge` class is the named STAGED next step (esbuild-wasm, cached as a κ-transform); a runtime backend is encoded but its calls reach nothing serverlessly (translating persistence→κ-store / inference→Q / egress→holo-gov is separate deeper work, [0090](0090-holo-q-remote.md)); imported code is UNTRUSTED, stays sandboxed + conscience-gated ([0033](0033-holo-constitution.md)) — arbitrary-GitHub-code is the real attack surface, treated as such (no new privilege; the studio adopts the sealed source like any paste); the two κs of [0091](0091-holo-q.md) (fabric compute vs published app) still hold, the user sees one. Composes 0091·0051·0089·0082·0090·0080 + `holo-blocks-repo.mjs`/`holo-gov.js`; grounded in Laws L1/L3/L4/L5; mint only `hosc:Import`. Pending: the `forge` bundler-class execution (the next class up), a `#holo-import` conformance row, and the os-closure re-lock (`holo-import.mjs` + `holo-q-mux.js` + `holo-blocks-repo.mjs` + `shell.html`) | Proposed (pure core + static/SPA loop + live Create-studio wiring landed + Node-witnessed 32/32 + verified live in the booted shell; Forge class + `#holo-import` row + κ re-lock staged) |
| [0090](0090-holo-q-remote.md) | Holo Q Remote Model — a non-local LLM is reached through ONE governed, witnessed substrate operation, NOT app-level fetch. Opened by "is it worth vendoring a unified-LLM gateway (mozilla-ai/any-llm) for seamless non-local access?" — no (it's Python, routes to servers, and frictionless egress is the wrong default), but the real finding is sharper: **boost as it ships ([0087](0087-holo-trinity.md), `holo-q-boost.js`) is ALREADY the Law violation a gateway would formalize** — a bare `window.HoloBoost` raw-`fetch`ing `api.anthropic.com` from page scope (parallel network → breaks L4; engine named by URL → breaks L1; key visible to the page; no audit trail). The apparent L5 objection (non-reproducible bytes) is false by inspection: the DEVICE tier (Qwen2.5 sampling) is ALSO non-reproducible yet nobody says it breaks L5 — because L5 is *re-derive every received byte against its κ*, byte→κ verification NOT function reproducibility; a remote model is identical on that axis. The ONLY real delta is L1/L4 egress (a governance problem), so this brings the one egress the OS already does UNDER the Laws. THE MODEL: (1) the engine is a content address `{wireFormat,modelId}` (`providerKappa`, JCS+SHA-256), URL+key live ONLY in the host VAULT, never content-addressed/witnessed/posted to an app (L1); (2) egress is ONE brokered op `infer({app,providerK,messages})`, the network an ingest boundary not a parallel runtime — only the receipt κ enters the store (L4, the precise repair of boost's hole); (3) every answer is content-addressed (`responseKappa`, identical→same κ, L3) and mints a MANDATORY re-derivable PROV-O `holoq:InferenceReceipt` — P2 (Provenance duty, [0033](0033-holo-constitution.md)) makes "no receipt" a `leavesNoAuditTrace` violation, so a receiptless answer is never admitted; it slots into Holo Prov ([0082](0082-holo-prov.md)) `walkLineage` so a remote-assisted artifact carries honest lineage forever; (4) the conscience gates the OUTGOING prompt — **P5 (red line) HARD-BLOCKS PII leaving the device**, P6 blocks classified, P4 blocks with no grant (the grant IS the consent envelope); (5) capabilities are default-deny, HOST-asserted (the `holo-gov.js` `byWin` recipient, un-forgeable), scoped + revocable, and an app that never asks keeps the 0-network core-loop guarantee. SUBSUMES boost (LANDED): `holo-q-boost.js` rewritten as the Anthropic specialization of the broker — its raw fetch/SSE → `adapters.anthropic`, the API key moved OFF `window`/the sampler INTO the vault, every generation now passes the egress gate + mints a receipt (`.lastReceipt`); `window.HoloBoost` stays the SAME `(messages,opts)→async-iterable of deltas` sampler so `shell.html` + `holo-q-codegen.js` need NO edit, only boost's nature changed (vaulted key · governed · receipted). The any-llm value (normalize Anthropic·OpenAI·Ollama) is three PURE adapter functions, zero deps, no SDK/Python. Landed `os/usr/lib/holo/q/holo-q-receipt.mjs` + `holo-q-adapter.mjs` + `holo-q-remote.mjs` + the boost rewrite, Node-witnessed 30/30 (`holo-q-remote-witness.mjs`, MOCK fetch + REAL conscience: 3-format shaping + SSE reconstruct · provider κ has no URL · response κ dedup · receipt re-derives + tamper-refused incl. the tier label · PII→P5 hard-block · no-grant→P4 block · admit+mint · key never leaves the vault · revoke→default-deny · boost drives the unchanged codegen seam as mode "boost" + mints a receipt + blocks PII + key not on the page-held sampler). CROSS-FRAME PROTOCOL + LIVE SHELL HOST-INSTALL LANDED — `holo-q-remote-client.mjs` (app-side, holds no key/URL) + `serveRpc`/`listProviders` (host dispatch, host-asserted `app`) + an additive `holo-gov.js` `q.remote.*` delegation branch (→ `window.HoloQRemoteServe`, streams `holo-privacy:delta`, fail-closed); the shell installs ONE host broker (`window.HoloQRemote`/`HoloQRemoteServe`, lazy conscience) and boost registers into that SAME broker (`createBoostSampler({broker})`) so the shell frame + every iframe app share one governed vault. Node-witnessed 31/31 (incl. no-secret discovery · grant stamped to the host-asserted id · streamed deltas + verifiable receipt · PII hard-blocked at the host · the key NEVER crossing the bus · ungoverned frame denied · boost via the shared broker) AND VERIFIED LIVE in the booted shell (`holo-os`/8300: globals install no-error, conscience sealed, in-page real-gate round-trip — no-grant→P4, granted→streams "Paris."+receipt re-derives/tier "remote", PII→P5, key absent from result; HoloQRemoteServe denies ungoverned + serves no-secret discovery); `holo-app-governed-witness` still passes. HONEST: the receipt proves a gated egress with these exact bytes under a sealed conscience but NOT that the bytes are genuinely from the named model (TLS+host integrity, outside the substrate — same trust the DEVICE tier puts in its κ-disk; provider response-signatures can be pinned to strengthen it) — still strictly more honest than boost's no-receipt-at-all; postMessage streaming adds marginal latency (transferred MessagePort); the key still lives in the browser but is CONTAINED host-side (app holds nothing), a remote build is not serverless (boost's named trade). Composes 0033·0082·0087·0052·0024·0025 + `holo-gov.js`; grounded in Laws L1/L2/L3/L4/L5; mint only `holoq:InferenceReceipt`/`:Provider`/`:Request`. Pending: a polished key-custody/consent UI (the console `HoloBoostInstall(key)` path already registers a provider into the shared broker), a `#holo-q-remote` conformance row, and the os-closure re-lock (`shell.html`/`holo-gov.js`/`holo-q-boost.js` + the new q modules' κs moved) | Proposed (egress seam + boost subsumption + cross-frame protocol + live shell host-install landed + Node-witnessed 31/31 + verified live in the booted shell; key-custody UI + `#holo-q-remote` row + κ re-lock staged) |
| [0091](0091-holo-q.md) | Q — the ONE unified surface for hologram-native intelligence: a single door every human, app, and external agent touches, composing the whole Holo Q spine behind one verb. The reflection on the stack ([0087](0087-holo-trinity.md)) left a residue — even after the Create build rode the spine, intelligence was still a SCATTER of globals (`HoloTrinity`·`HoloMind`·`HoloBoost`·`HoloQOrchestrator`·`HoloConscience`): a human, an app, and an external agent each had to know which global to reach, and there was NO single governed door for non-human callers (an external agent would be handed interior objects — confusing AND a hole). FIX: **Q**, a thin facade that COMPOSES (never replaces) the stack as one calm object. (1) ONE verb for humans/apps/internal — `Q.create(intent)`/`Q.ask(intent)` delegates to `trinity.create` over the κ-memo fabric ([0084](0084-holo-q-mux.md)/[0085](0085-holo-q-fabric.md)) → a repeat ask is O(1), auto-perceived ([0086](0086-holo-perception.md)) with a real rebuild thunk; the Create studio's `holoBuild` now calls `window.Q.create` (studio and agent take the SAME path). (2) ONE GOVERNED door for external/non-human entities — `Q.agent(request,{caller})`, FAIL-CLOSED: the conscience ([0033](0033-holo-constitution.md)) must accept, every act seals a receipt (who·verb·effect κ), the interior is NEVER handed out; no gate → denied by default, only explicit `allowUngoverned` lets an internal caller through; an accepted act's `effectKappa` EQUALS the human-verb κ (one spine, one result). (3) UBIQUITOUS — `installQ() → window.Q` singleton, every app inherits the one Q. (4) self-healing (`Q.perceive`/`Q.improve` over the loop) + BOUNDED adaptation (`Q.remember`/`Q.context` = recent-intent continuity + 👍/👎 bias), labelled honestly. Landed `os/usr/lib/holo/q/holo-q.js` (`createQ`/`installQ`), installed by `holo-q-trinity-ui.js`, Node-witnessed 17/17 (`holo-q-unified-witness.mjs`: one verb O(1) repeat + same κ · agent fail-closed with no gate · refused on block · accepted→receipt whose effectKappa===human κ · ungoverned escape · context tracks intents+feedback · perceive/improve/route/describe) AND verified live in the booted shell (`holo-os`/8300: `window.Q` installs + composes the trinity, `Q.create` cold→hot repeat 1ms, `Q.agent` routes through the real sealed conscience + receipts, the studio build flows through `window.Q`). HONEST: Q is a FACADE — its power is exactly the spine's; the win is the single coherent governed door. The external door is a governed FUNCTION; wiring it to a real network transport (MCP/postMessage) is a SEPARATE opt-in (the actual attack surface), NOT opened here. Adaptation is a preference bias, not a learner; a build is on-device/template unless boost is installed (then a governed network call, [0090](0090-holo-q-remote.md)) — Q inherits that trade, doesn't erase it; the studio still seals two κs (display vs compute), unifying them is the follow-on. Composes 0087·0084·0085·0086·0033·0090; grounded in Laws L1/L3/L5; mints nothing of its own (returns the trinity's κs + a lightweight agent-door receipt). Pending: the external-transport binding (opt-in), display κ === compute κ, a `#holo-q` row, os-closure re-lock (`holo-q.js`/`holo-q-trinity-ui.js`/`shell.html`) | Accepted (unified surface landed + Node-witnessed 17/17 + verified live in the booted shell; external-transport binding + κ-unification + `#holo-q` row + re-lock staged) |
| [0089](0089-holo-scene.md) | Holo Scene — the GENERAL composed-holospace manifest: any κ-app/experience that arranges κ-objects (a dashboard, a multi-pane experience) is ONE κ with **render · link · remix · share · provenance** over its LIVE STATE, not just its source. Answers "does desktop-as-holospace ([0088](0088-holo-home.md)) apply to every κ-app that builds/runs/shares in an isolated holospace?": the five verbs are ALREADY universal (they follow from content-addressing — every κ-app renders/links/remixes-source/shares/tracks-lineage; 0088 only closed the desktop, the one holospace that wasn't a document). The real line is **source vs running state**: `source κ → screen` is always re-derivable, but a running app's live DOM ≠ its source, so you can remix its source exactly but can't snapshot its live STATE into a κ UNLESS the app models state as content. The scene manifest IS that discipline, now generalized: `defineScene({type})` lets an app declare its kind once; `objects[]` carry a DEFINED minimal `state`; `render(manifest) ⇄ serializeWorld(live)` are INVERSE (Atlas isomorphism [0043](0043-holo-atlas.md)) — so an app can round-trip its RUNNING STATE to a κ (runtime made re-derivable: share/remix the live thing, not just the file). `sceneKappa` over content (kind⊕background⊕objects⊕props) → same composed scene → same κ (Law L5, order-independent); `remix` → child κ + `prov:wasDerivedFrom` ([0082](0082-holo-prov.md)); `walkLineage` → genesis. ISOLATION preserved — a scene references κ-objects (each stays sandboxed), it's the composition not a hole in the boundary. The desktop ([0088](0088-holo-home.md)) is now the FIRST specialization (home = `holo:HomeScene` over the shared engine, nothing lost). Landed `os/usr/lib/holo/holo-scene.mjs` + refactored `holo-home-scene.mjs`, Node-witnessed 10/10 (app declares kind · order-independent re-derive · render→serialize round-trips a composed app WITH live state to the same κ · live object state survives the round-trip · remix child+prov · walkLineage · home still round-trips via the shared engine). HONEST: background is a ref/seed not pixels (content-exact not frame-exact); `state` is a DEFINED minimal set named per object kind (the design work each app owes), not arbitrary live DOM; opting in is the app's choice (naive app remixes SOURCE, scene-declaring app remixes STATE). FIRST STATE SCHEMA LANDED — `holo-scene-state.mjs` `holo:widget`, grounded in the Holo Widgets runtime (`serialize()` already persists {type,config,hidden}, keeps out transient `_state`): a widget board round-trips its LIVE STATE end-to-end (Node-witnessed 8/8 — board→scene→render→serialize→same κ · a countdown tick changes nothing (transient excluded) · a config edit → new κ · remix → child κ+prov). Composes 0088·0082·0080·0043; mint only `holo:Scene` | Accepted (general engine + first state schema landed + Node-witnessed 10/10 + 8/8; remaining kind schemas + compositor/Create wiring shared with 0088, staged) |
| [0088](0088-holo-home.md) | Holo Home — the DESKTOP becomes a content-addressed holospace, so every holospace (now the home itself) is ONE κ-object and **render · link · remix · share · provenance** all follow from that single fact. An app is already a κ-document (verified: opening it into Create mirrors its exact source + FLIPs the live window into the preview + reverses on Done); the one hole was the desktop — the shell's live compositor (animated cosmos `canvas` + composited live κ-objects + dock), NOT a document, so it couldn't be mirrored/remixed/shared/tracked (entering Create from the empty desktop scaffolded "Hello, holospace"). FIX: make the desktop a deterministic render of a small canonical **manifest** that REFERENCES — never serializes — the screen: wallpaper as a **seed** κ ([0080](0080-holo-cosmos.md) — the cosmos re-derives from the seed, never a pixel snapshot), each placed κ-object as `{k,x,y,w,h,state?}` (ref + layout + a DEFINED minimal round-trip state), dock/theme config κs; objects canonicalized by (k,x,y) so the κ is a pure function of the SET + layout (order-independent); sealed (JCS+SHA-256) to the home κ. THE KEY PROPERTY: `render(manifest) ⇄ serializeWorld(desktop)` are INVERSE (the Atlas isomorphism [0043](0043-holo-atlas.md)) — the desktop and its κ are one object, two views, so the live screen and the manifest can't disagree (a real doc, not a mirrored fiction). `sceneKappa` addresses content (same desktop → same κ on every device, Law L5 → share = share the κ, recipient re-derives exactly, no server/sign-in); `remix(parent,edit)` → a child κ linked `prov:wasDerivedFrom` the parent ([0082](0082-holo-prov.md)); lineage is metadata (content convergence: undo → parent κ); `walkLineage` → genesis. Once the boot is refactored, `csResolve`'s empty-desktop case returns the home κ and the existing window→preview FLIP + Done-reverse apply to the desktop with NO special case ("the original canvas shrinks into the preview" becomes literally true, no faked content). Landed `os/usr/lib/holo/holo-home-scene.mjs`, Node-witnessed 11/11 (re-derive order-independent · render-pure · render⇄serialize round-trips to the same κ · remix child+provenance · content convergence · walkLineage). DESKTOP↔SCENE BRIDGE LANDED — `holo-home.mjs` serializes the shell's LIVE desktop (`desktop.doc().world` nodes + `HoloWidgets.snapshot()` + wallpaper seed + layout) → ONE `holo:HomeScene` κ via the general engine ([0089](0089-holo-scene.md)); round-trips (worldToScene⇄sceneToWorld, mixed app-nodes+widgets, same κ); gains a PROVENANCE chain (remix desktop → child κ wasDerivedFrom + walkLineage) the shell's own `desktop.kappa()` repo-κ LACKED + per-object live state (widget schema, a runtime tick → same κ); Node-witnessed 7/7. The READ half (live desktop → remixable κ with lineage) is safe + done. HONEST: wallpaper is a seed not pixels (content-exact, not frame-exact); manifest = refs+layout+defined-state, not arbitrary live DOM; the LOAD-BEARING remaining work is the COMPOSITOR REFACTOR (desktop boot → `render(homeκ)` over holo-world-rw+holo-render + csResolve/preview wiring, a guarded migration, not a one-shot edit) — until then the empty-desktop scaffold stands, no faked mirror. Composes 0080·0082·0043·0052·0024·0025·0089; mint only `holo:HomeScene` | Accepted (scene-manifest model + desktop↔scene bridge landed + Node-witnessed 11/11 + 7/7; compositor boot refactor + csResolve/preview wiring + witness row staged) |
| [0087](0087-holo-trinity.md) | Holo Trinity — the UNIFIED, OS-wide **create·exist·perceive** experience: one loop every native app inherits by CONFORMING, not wiring, behind one `mount()` + one `create()`, with the trinity INVISIBLE (the user just sees: *everything you make is instant, private, and quietly gets better*). Composes the six Holo Q modules (mux [0084](0084-holo-q-mux.md) · fabric/renderer [0085](0085-holo-q-fabric.md) · perception/loop [0086](0086-holo-perception.md) · orchestrator [0081](0081-holo-mind.md)) into the triad the orchestrator↔interpreter synthesis derived: **CREATE** (immanent — the interpreter's act via the fabric; sovereign + UNGATED + fast, sealed to a κ), **EXIST** (transcendent — the κ-substrate grounds every output, content-addressed + memoized cross-app O(1); the commons where observer meets observed), **PERCEIVE** (omniscient — perception registers code+visual faces + the loop heals drift/optimizes/learns; conscience-GOVERNED, edits only what EXISTS). THE ETHIC IS THE ARCHITECTURE: creation sovereign, improvement governed; create-then-edit enforced structurally (each create SEALS to κ before perceive touches it — streaming is the same loop at micro-granularity, never simultaneity). Complexity abstracted: shell installs ONE OS-wide trinity; apps conform (the renderer `onFinal` auto-feeds the visual face, the fabric the code face); the whole surface is mount-once + create-per-act, exist+perceive automatic; the six modules vanish behind two calls, the loop below the waterline. Continuously self-improving via a cheap ambient ticker (scene-κ makes an unchanged pass ~free), healing across ALL apps in one scene, governed, learning (a trace per improvement); the heal re-creates from CURRENT state (a `params` thunk → correct re-derivation). Landed `os/usr/lib/holo/q/holo-q-trinity.js`, Node-witnessed 9/9; VERIFIED LIVE in the browser (one mount → create flows through all three modes, objects coherent automatically; identical create → O(1) hot replay 0.17 ms; a state change drifts every object; the governed loop self-heals to the CURRENT state ("Welcome hero · violet"), records a trace; 0 network calls). HONEST: coverage = conformance (an app bypassing the OS seams is invisible until it mounts); `create` is task-agnostic (provider/task); ambient ticker is opt-in; quality bounded by the on-device specialists. SHELL-WIDE INSTALL LANDED — `holo-q-trinity-ui.js` (twin of holo-mind-ui.js) imported once by `shell.html` installs the ONE OS-wide `window.HoloTrinity` + starts the ambient ticker (conscience gate resolved lazily); verified live in the booted shell (`window.HoloTrinity` present beside HoloMind, conscience wired, real mount→create through all three modes, object coherent at κ 5885228b…, repeat O(1) 0.09 ms). WIRED INTO CREATE MODE — the studio's build flow routes a natural-language prompt through HoloTrinity.fabric (κ-memoized) into its content-addressed double-buffered preview, perception tracking code+visual; verified live in the real Create studio (a prompt → a themed holospace builds in the preview at 0.31 ms / holo://…, same prompt rebuilds at 0 ms O(1) cache, a new prompt rebuilds at 0.28 ms — a simple prompt on the left generates a real content-addressed app on the right in real time, serverless). Composes 0084·0085·0086·0081·0052·0024·0025·0033; mint nothing | Accepted (composition root + shell install + Create-mode wiring landed + Node-witnessed 9/9 + verified live in the studio; full app conformance + a real generative specialist + witness row + κ re-lock pending) |
| [0086](0086-holo-perception.md) | Holo Perception — the orchestrator ([0081](0081-holo-mind.md)) SEES the whole holospace by READING the κ-graph it already produces, NOT by screenshotting pixels: the popular computer-use stacks (Open Interpreter · OmniParser · Qwen2.5-VL · UI-TARS · Skyvern) all RECOVER structure from pixels via a heavy server-bound VLM — a problem this substrate doesn't have (every object is content-addressed; the renderer [0085](0085-holo-q-fabric.md) already stamps every on-screen fragment with a render-κ), and they fail both hard constraints (very low latency · 100% serverless) for the interior loop (server-bound · 100s ms–s · lossy/non-deterministic → can't re-derive, breaks L5). THE MODEL: each κ-object has a **code face** (source/state/compute-output κ, fed by the fabric/orchestrator dispatch) AND a **visual face** (rendered-fragment κ, fed by the renderer's `onFinal`), joined on one logical id → the orchestrator perceives both code and screen in real time; a change in either emits an **O(1) delta** tagged with the axis that moved (`code·visual·both`); a whole-scene κ skips an unchanged pass instantly; **DRIFT** (code newer than visual — `codeSeq>visualSeq`, deterministic, no clocks) is the real-time self-improvement signal, `coherent` (both faces fresh) the goal — and since the substrate makes render a deterministic fn of code, coherent is the DEFAULT, drift only on lag/failure. Closes the loop: observe (scene delta) → evaluate (drift + fabric latency/cache-hit telemetry + receipt validity) → act (mux/fabric) → re-observe, feeding `reevaluate` ([0084](0084-holo-q-mux.md)) + the trace corpus ([0081](0081-holo-mind.md) evolve) under the conscience/succession gate. Vision (a small on-device VLM) is kept ONLY at the **raster edge** (non-κ content — external iframe/image) via the mux "vision" task, its output sealed back as a κ-object so the interior stays deterministic. Landed `os/usr/lib/holo/q/holo-q-perception.js`, Node-witnessed 12/12 (two faces · axis deltas · O(1) no-op · scene-κ skip · drift fires+heals · integrated orchestrator-code + renderer-visual → coherent); VERIFIED LIVE in the browser preview (creator demo: 2 prompts → 2 κ-objects, both faces, all coherent, shown on screen; a code-edit-without-rerender flags drift, heals on re-render). HONEST: sees only κ-addressed/rendered content (raw third-party canvas/iframe invisible until the edge VLM lifts it — the one slow/non-deterministic place pixels enter); full coverage needs every producer to register (renderer auto-covers visual, orchestrator auto-covers code); self-improvement is only as good as its objective. Composes 0081·0084·0085·0052·0024·0025·0033; mint nothing | Accepted (perception landed + Node-witnessed 12/12 + verified live; full producer coverage + raster-edge VLM + witness row pending) |
| [0085](0085-holo-q-fabric.md) | Holo Q Fabric — content-addressed compute for the specialist fleet ([0084](0084-holo-q-mux.md)): every model call is **κ-memoized** (`κ(model ⊕ input ⊕ params)` → **O(1)** replay, no compute, no token re-stream), **streamed** (one async-iterable `run()` fits instant specialists (embeddings → a single final) AND generative ones (live `delta` events — append for autoregressive, block-refine for diffusion [0083](0083-holo-q-diffusion.md) — then a sealed final)), and **κ-addressed** (every result carries its content κ; `renderKey()` lets a UI keep `Map<κ,node>` and skip re-rendering identical output). The layer that converges a universe of small models toward a real-time, LLM-like rendered experience: a fleet is only "real-time" if repeated + structural work is FREE, and on this substrate it can be — a model output is bytes (a content-addressed κ-object) and a fixed-params call is deterministic, so an answer is computed ONCE in the life of the system and every identical ask after is a lookup. Generalizes the substrate's one content-addressed-compute idiom (Holo Q's answer memo [0052](0052-holo-q.md), Holo Mind's `memoKey` [0081](0081-holo-mind.md), the κ-store `holo-store.js` Law L3/L4) across the WHOLE helper surface, built ON those rails not beside them; the hot path collapses to memory speed, the cold path streams — the shape of a fast LLM UI as a substrate primitive. Holo Mind ([0081](0081-holo-mind.md)) discovers (mux) · applies · self-improves (`reevaluate`) over it; each output a re-derivable conscience-gated receipt. Landed `os/usr/lib/holo/q/holo-q-fabric.js`, Node-witnessed 11/11 (cold seals a κ · identical ask → O(1) hot SAME κ, provider runs ONCE · distinct input/params → distinct κ · streaming deltas → sealed final, replay = one instant final · durable κ-store seal · renderKey reuse). EXPLICIT ORCHESTRATOR BINDING LANDED — `holo-q-orchestrate.js` reuses the REAL Holo Mind `runLoop` (intent→plan→gate→dispatch→seal) with dispatch wired to the fabric: helper tasks become roster verbs (Law L4), the conscience gates each, and the receipt `effectKappa` IS the fabric's content κ; end-to-end Node-witnessed 12/12 — sealed receipt re-derives byte-for-byte (L5), a repeat hits BOTH O(1) layers (plan memo + compute memo: **1.24 ms vs 32.7 ms cold**, specialist runs once), the loop makes **zero network calls** (100% serverless), a blocked verb seals nothing. κ-ADDRESSED RENDERER LANDED — `holo-q-render.js` (structured-DOM only, never raw innerHTML; a witness caught+fixed a style hole that allowed `url(javascript:)`) paints the fabric's streamed output live and reuses identical fragments by content κ in O(1); Node-witnessed 8/8. SHOWN LIVE in a creator-mode demo (`studio-demo.html`) verified in the browser preview: prompt → a hero builds in the preview pane (**0.88 ms cold**), a repeat is a **cache hit + render-reuse at 0.07 ms** (both O(1) layers), **0 network calls** (serverless, a fetch-guard counts on screen). HONEST: the next step is mounting it into the REAL Create studio (the showcase proves the loop standalone) + a WebGPU node path; the input→κ index is in-mem in v1 (sealed bytes durable, cross-reload O(1) needs it persisted); determinism/memo hold at fixed params (a sampled call bypasses the seal); a memo is only as fresh as its key (time/external state must fold in). Composes 0052·0081·0084·0024·0025·0083; mint nothing | Accepted (fabric + explicit Holo Mind orchestrator binding landed + Node-witnessed 11/11 + 12/12; κ-addressed renderer + real model planning + persisted index + witness row pending) |
| [0084](0084-holo-q-mux.md) | Holo Q Mixture-of-Specialists — the OS helper tasks (the model-router surface: Vision · Web extract · Compression · Session search · Skills hub · Approval · MCP · Title gen · Curator) stop defaulting to one big model and each AUTO-DISCOVERS the best small specialist the open web offers, run on-device + proven by receipt: **"auto" stops meaning "use the main model" and starts meaning "the right tiny mind for this exact job."** Driven by the native orchestrator ([0081](0081-holo-mind.md)) as a discover→select→act→verify loop. THE FACTORING (cheap/serverless vs heavy/honest): DISCOVER = one cheap Hugging Face Hub API call per task (`/api/models?pipeline_tag=…&sort=downloads` — a browser `fetch`, no server, no auth); SELECT = a PURE deterministic ranking over METADATA only (hard gate on browser-runnability — an `onnx`/`transformers.js`/`gguf` marker, others dropped — then pipeline match · size-fit within a cap (smaller better, over-cap disqualified) · log-downloads+likes · open-license; ties by id → same Hub response always yields the same pick, Law L5) — **no candidate is downloaded to be judged** (that would break fast + serverless); the chosen specialist is a PLAN, not a loaded model; BIND = the plan streams as a content-addressed κ-disk ([0052](0052-holo-q.md), every sector re-derived), adapts to the engine seam its job needs (embed·classify·generate·vlm), and registers in a PER-TASK provider registry — the QVAC `useBrain()` idiom ([0067](0067-holo-qvac.md)) widened from one active brain to one-per-task; VERIFY+FALLBACK = each output seals the SAME decode-agnostic InferenceReceipt + conscience gate ([0033](0033-holo-constitution.md)/[0083](0083-holo-q-diffusion.md)), and with no browser-runnable specialist (or no WebGPU) `routeTask()` returns the main model — **never blocks, never fakes** (twin of the diffusion seam's honest-pending). THE MAGIC IS THE SIMPLICITY: the router surface stays exactly as plain — a list of tasks each saying "auto" — but "auto" is now quietly genius (a subtle name·size·source cue; one tap reveals the why + receipt); every hard part hidden. Selection + routing landed in `os/usr/lib/holo/q/holo-q-mux.js`, Node-witnessed 13/13, VALIDATED LIVE against the real Hub (surfaces `BAAI/bge-small-en-v1.5` for recall, `Falconsai/text_summarization` for page summary, `distilbert-…-sst-2` for approval). Honest costs: only browser-runnable models are eligible (the in-tab slice is the real catalog); size is name-ESTIMATED (a hard cap needs a deferred per-pick detail fetch); the per-task provider CONSTRUCTION (ONNX/GGUF → a running embed/classify/generate provider) is the real remaining work — selection/routing done, execution adapters not; cold-start hidden by the κ-cache + fallback, not erased. FIRST EXECUTION ADAPTER LANDED + PROVEN — `holo-q-embed.js` takes **Session search** all the way: auto-discovers bge-small, runs it on-device through the OS's already-vendored transformers.js (auto WebGPU/WASM · ORT-in-worker · q8), binds it as the task provider, seals a re-derivable embedding receipt, with a content-addressed cache (identical text → O(1), self-optimizing telemetry) and a `reevaluate()` swap-recommendation the orchestrator ratifies (self-improving); `autowire()` falls back to the deterministic reference embedder so recall never blocks. Proven through the real runtime (`Xenova/bge-small-en-v1.5`, q8): 384-dim, **≈6 ms/embed**, reproducible (same text → identical vector), correct semantic recall (on-topic 0.73 vs off-topic ~0.35, ranks #1); Node-witnessed 8/8. Composes 0052·0067·0081·0017·0033·0024·0025·0083; mint nothing | Accepted (selection + routing + the Session-search/embedding adapter landed + proven; classifier/generative/vlm adapters + shell wiring + witness row pending) |
| [0083](0083-holo-q-diffusion.md) | Holo Q Diffusion — a masked-diffusion LM (Dream-org's Dream-7B, a Qwen2.5-7B backbone) is a SECOND decode family under the one inference transform ([0052](0052-holo-q.md)), not a second engine: the receipt is **decode-agnostic**, so block-by-block bidirectional UN-masking re-derives exactly as token-by-token greedy does. The hidden assumption in Holo Q's proof was that decode is autoregressive; this records the measured finding that it need not be — with a fixed step schedule + confidence-ranked unmasking + temperature 0, diffusion decode is fully deterministic (validated: Dream-7B at int4 on CPU, `Models/Dream/holo_dream_q4_stream.py`, two passes → **byte-identical output κ** `2a67997cb7b1725d`), and arguably cleaner since the forward-pass count is fixed by `steps`, not output length. THE FACTORING: the deterministic control plane — the `linspace(1,eps,steps+1)` schedule, the `round(masked·(1−s/t))` per-step finalize count, max-softmax confidence ranking (ties by position → pure), greedy token — lives in `os/usr/lib/holo/q/holo-q-diffusion.js` (re-derivable in Node + browser, the Atlas isomorphism), and ONLY the heavy 7B forward is delegated to an injected `kernel.logits(ids)` (the deferred WebGPU masked-diffusion kernel, where the per-step parallelism that makes diffusion fast on a local GPU lives). Bound behind the EXISTING `HoloQVAC.useBrain()` — no new façade, no new receipt; the spec gains a `DECODE` family list + a `DREAM_V0_INSTRUCT_7B` descriptor (`decode:"diffusion"`, `provisioned:false`). Honest until bound: with no kernel, `generate()` THROWS a structured pending error → `completionError`, never a faked token (Law L5, twin of `unprovisioned()`/`useHoloQ()`). Enables a faster local brain (a few parallel steps vs one pass/token), native fill-in-the-middle + constrained generation for the verifiable coder, and **verification by diversity** (Dream shares Qwen2.5's weights but decodes differently → an AR and a diffusion derivation cross-check) — the decode-side analog of what hologram-ai's ONNX compiler (ADR-0017) did for formats. Node-witnessed 6/6 (schedule · re-derivation · honest-pending · provenance). Honest costs: the WebGPU forward kernel is real GPU work (until it lands Dream does not generate in-tab); validation ran at int4 RTN (≈0.12 relerr — AWQ/GPTQ + more steps are the quality path) because the reference box can't hold 15 GB bf16; the int4→κ-disk pass + a required `#holo-q-diffusion` witness row + the os-closure re-lock are owed. Composes 0052·0067·0025·0024·0051·0043·0033; mint nothing | Accepted (seam landed + Node-witnessed; WebGPU kernel + witness row + κ-re-lock pending) |
| [0082](0082-holo-prov.md) | Holo Prov — provenance is a binding, in-tab-verifiable feature of EVERY holospace: an app proves its own LINEAGE by walking `prov:wasDerivedFrom`/`prov:wasRevisionOf` to genesis and, at each hop, asserting the edge (`parent.lock.root === the κ the child claims`) AND re-deriving the parent's WHOLE closure (re-hash every file to its κ, Law L5) — a forged parent resolves to no indexed app or its bytes don't re-derive, caught structurally. The verify core (`walkLineage`/`reDeriveClosure`) is a PURE function of injectable resolvers, so the browser app and the (deferred) Node witness run identical logic (the Holo Atlas isomorphism, [0043](0043-holo-atlas.md)). Two surfaces, ONE shell binding, no per-app code: an in-tab lineage badge (`window.HoloProv`, opt-out `<meta name="holo-prov" content="off">`) + a titlebar provenance cue (`⛓ v<n>` `↩` for a remix, twin of the ownership cue) + the live provenance HYPERGRAPH (nodes = holospaces, edges = cross-app remix ⊕ per-app version steps) the shell maintains via `register(node, el)` — the FOUNDATION for Holo Indexer, the substrate's answer to a blockchain indexer (broadcast truth it re-derives, never a feed it trusts). LANDED + shell-bound (`shell.html`); witness `tools/holo-prov-witness.mjs` + `#holo-prov` row DEFERRED. Resolves the old ADR-0076 code-citation collision (Heal keeps 0076). Composes 0025·0043·0039·0053·0064; mint nothing | Accepted (shell-bound; witness + row pending) |
| [0087](0087-holo-trinity.md) | Holo Trinity — the unified, OS-wide create·exist·perceive experience: one loop every native app inherits by conforming, behind one mount and one verb, the trinity invisible | Accepted |
| [0088](0088-holo-home.md) | Holo Home — the desktop becomes a content-addressed holospace: the home itself is ONE κ-object, with render·link·remix·share·provenance following from that single fact | Accepted |
| [0089](0089-holo-scene.md) | Holo Scene — the general composed-holospace manifest: any κ-app that arranges κ-objects is ONE κ over its LIVE STATE (not just its source); the desktop (0088) is its first specialization | Accepted |
| [0090](0090-holo-q-remote.md) | Holo Q Remote Model — a non-local LLM reached through ONE governed, witnessed substrate operation (the engine is a content address, never a URL); every answer mints a re-derivable receipt and the conscience gates the egress | Proposed (egress seam + boost subsumption + shell host-install landed + Node-witnessed 31/31, verified live; key-custody UI + `#holo-q-remote` row pending) |
| [0091](0091-holo-q.md) | Q — the one unified surface for hologram-native intelligence: a single door every human, app, and external agent touches, composing the whole Holo Q spine behind one verb | Accepted |
| [0092](0092-holo-import.md) | Holo Import — paste a GitHub URL, get a running content-addressed Holo app: import is a deterministic encoder onto the existing publishSource/Q spine, honest about where "serverless" ends | Proposed (pure core + static/SPA loop landed + Node-witnessed 32/32, wired into the Create studio + verified live; Forge bundler class + `#holo-import` row staged) |
| [0093](0093-holo-import-agent.md) | Holo Import → agent-native — an imported repo (0092) is auto-wired into the MCP + REST seams every holospace already exposes, so any AI agent or holo app can fetch · re-derive (L5) · run · share · discover it with zero author effort | Proposed (agent surface + serverless transport bridge + Forge bundler + governed ingest landed + witnessed 45/45·38/38·12/12·6/6, verified live; OpenAPI/postMessage/non-esbuild staged) |
| [0094](0094-holo-identity.md) | Holo Identity — one sovereign, post-quantum κ-rooted identity for humans and agents | Proposed (crypto foundation landed + witnessed; surface unification staged) |
| [0094](0094-holo-hub.md) | Holo Hub — a Shopify-class, 100% serverless, κ-anchored commerce experience where a PRODUCT IS a holo app addressed by its `did:holo` κ; composes Own·Settle·Atlas·Scene and adopts Medusa's commerce model as a byte-pinned spec | Proposed (design only) |
| [0095](0095-holo-devtools.md) | Holo DevTools — the F12 developer environment native to the κ-substrate (vendor the Chrome DevTools frontend unmodified, build the κ-CDP backend), equally usable by humans, AI agents, and Holo Q | Proposed — design only (CDP↔κ flow-map; seven-stage build plan specified, not landed) |
| [0096](0096-holo-q-model-registry.md) | Holo Q model registry — one any-browser coder brain + a fused specialist set (the Function→Model→κ doctrine) | Accepted |
| [0097](0097-holo-factory.md) | Holo Factory — the software factory as one verb, native to Q | Accepted |
| [0098](0098-q-fuse.md) | Q.fuse — a serverless compound model (panel → judge → synthesize) on the κ substrate | Accepted |
| [0099](0099-holo-recall.md) | Holo Recall — serverless κ-graph retrieval over the private corpus (hybrid + zero-LLM auto-link) | Accepted (landed + Node-witnessed 40/40; browser-verified) |
| [0100](0100-holo-hermes.md) | Holo Hermes — a native, in-tab, κ-anchored Hermes-compatible agent on the Q door (+ bidirectional interop) | Draft — design only (the GEPA-style evolving-skill loop landed + witnessed 9/9) |
| [0101](0101-holo-onnx-forge.md) | Holo ONNX Forge — the hologram-native ONNX compiler: precompile once, stream any model as κ-addressable, demand-paged blocks | Accepted (compiles + κ-verifies; ORT parity P3.7 open) |
| [0102](0102-holo-q-openrouter.md) | Q OpenRouter tier — frontier breadth as a governed, κ-memoized, receipt-sealed remote slot (pay-once intelligence), never an app-level fetch | Accepted (landed + Node-witnessed 26/26; live shell hook deferred) |
| [0103](0103-holo-onion-omnisearch.md) | Holo Onion Omnisearch — resolve Tor v3 `.onion` addresses as a first-class omnibar leg: cryptographically validated, κ-sealed, transport-honest | Accepted |
| [0104](0104-holo-session-persistence.md) | Holo Session — per-operator, device-local experience continuity | Accepted |
| [0105](0105-holo-workspace-sync.md) | Holo Workspace Sync — backup, recover, and resume the exact workspace on any device: serverless, content-addressed, transport-honest | Accepted |
| [0106](0106-holo-session-sovereign-guest.md) | Holo Session v2 — sovereign at rest, seamless for every user (guests included) | Accepted |
| [0107](0107-holo-onion-no-local-tor.md) | Onion without local Tor — a κ-substrate path to render `.onion` from a serverless tab, honestly | Exploration / design only |
| [0108](0108-holo-onion-seamless-managed-tor.md) | Seamless onion — managed Tor (the Brave model) + onion-web discovery, when anonymity is not the goal | Accepted (host logic + discovery + wiring landed + witnessed) |
| [0109](0109-holo-share-carriage.md) | Holo Share — the ❤️ Share verb as a left side-carriage | Accepted |
| [0110](0110-holo-playground-canvas.md) | Holo Playground 3.0 — the screen as a real playground | Accepted |
| [0111](0111-holo-boot-root.md) | Holo Boot Root — root the boot sequence in the substrate from byte 0 | Accepted |
| [0112](0112-holo-app-conformance.md) | Holo App Conformance — every app a self-contained, shareable, provenance-tracked κ-holospace | Accepted (witness landed; P6 coverage pending) |
| [0113](0113-holo-dial.md) | Holo Dial — dial peers by key, fetch objects by κ (a native Iroh) | Accepted (S0–S2 code + witness landed; live wiring + S3–S5 pending) |
| [0114](0114-holo-forge-unified.md) | Holo Forge Unified — any HuggingFace GGUF/ONNX → one κ-addressed `.holo` in-browser, with Q self-acquiring skills on demand; the honest fusion economics (shared-prefill + self-fusion, not free weight-dedup) | Proposed |

Related specifications live in [`../specs/`](../specs/) — e.g. the
[`did:holo` DID method](../specs/did-holo-method.md).

> A further ADR-style note, `CHROMIUM-BROWSER-ADR.md`, currently lives under
> `os/`; it is slated to move here once numbered.

## Writing a new ADR

1. Copy the shape of an existing ADR: **Status**, **Context**, **Decision**, **Consequences**,
   and the **external authorities** it conforms to.
2. Name it `NNNN-title.md` (next free number).
3. If it adds a conformance obligation, add its row to
   [`w3c-conformance.jsonld`](../../conformance/w3c-conformance.jsonld) and ship
   the witness with it — the decision is not done until it is witnessed (ADR-024).
