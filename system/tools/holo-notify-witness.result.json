{
  "spec": "Hologram OS has ONE notification surface (Holo Notify, holo-notify.mjs): holo.notify shows a transient, theme-coherent toast and ALWAYS files a durable, per-operator record into a persistent Notification Center rendered in the shared right side-carriage (createAside). The bell carries an unread badge; arrivals shimmer with the OS's κ-glyphs; motion is reduced-motion-aware. Q is a first-class sender with its own thread and a deliver-only seam (window.Q.note) that never generates text (Law L5). The two prior ad-hoc surfaces — the ephemeral #toast and the hardcoded 'Secure your account' banner — now flow through it.",
  "authority": "ADR-0109 (Holo Aside — the one right side-carriage) · ADR-0104/0106 (per-operator, device-local persistence axis) · ADR-0030 (Holo UI tokens) · ADR-0057 (readability floor) · golden ratio (holo-phi.css) · WCAG 2.2 (prefers-reduced-motion, status messages) · holospaces Law L5 (honest by construction — never fabricate a state) · verify by static analysis of the served module + shell + backup",
  "witnessed": true,
  "covers": [
    "notifications",
    "holo-notify",
    "notification-center",
    "carriage-reuse",
    "per-operator-persistence",
    "toast-unified",
    "backup-migrated",
    "q-sender",
    "deliver-only-seam",
    "unread-badge",
    "kappa-shimmer",
    "reduced-motion",
    "deep-link",
    "tokens",
    "golden-ratio"
  ],
  "passed": 21,
  "total": 21,
  "checks": {
    "holo-notify.mjs exists and is the notification surface": true,
    "exports mountNotifications + installs window.HoloNotify": true,
    "notify() shows a transient toast AND files a durable record": true,
    "a bare/transient toast is shown but NOT filed (history stays meaningful)": true,
    "history is per-operator + durable (localStorage UI-state axis, survives reload + re-sign-in)": true,
    "history is a recent window, not an unbounded log (capped)": true,
    "the Center reuses the ONE shared right side-carriage primitive (createAside)": true,
    "the bell carries an unread badge (obvious, not intrusive)": true,
    "a κ-glyph shimmer marks a fresh arrival (the OS's own sealed-byte vocabulary)": true,
    "token-driven, not hardcoded palette (the backup-banner anti-pattern)": true,
    "golden-ratio spacing tokens (φ) throughout": true,
    "calm motion: reduced-motion + mobile media queries honored": true,
    "mark-read · clear · sender filter": true,
    "tapping a notification deep-links to its source (omnibox / Q)": true,
    "Q is a first-class sender with its own thread": true,
    "Q ↔ Center seam delivers a handed note, never generates (Law L5)": true,
    "an honest welcome note seeds the Q thread once per operator (static, not model output)": true,
    "the shell routes toast() through the unified surface": true,
    "the shell mounts the Notification Center": true,
    "the backup nudge is migrated to a persistent, actionable Backup notification": true,
    "the hardcoded-hex backup banner is gone": true
  }
}
