{
  "@type": "earl:TestResult",
  "spec": "Hologram Homepage — ONE familiar portal door, delivered as a STREAMED κ-page: a thin host resolves a page κ-object by name from a content-addressed manifest (every page + every κ-UI component named by its did:holo content κ, Law L1) and mounts it through the κ-render spine, which verifies each object on load (Law L5). Golden-ratio (φ) layout; fidelity-adaptive prefetch; mobile-conformant (WCAG 2.2 / MD3).",
  "authority": "ADR-0088 · κ-page migration (holo-render substrate) · holospaces Laws L1/L5 · WCAG 2.2 · MD3",
  "witnessed": true,
  "passed": 16,
  "failed": 0,
  "covers": [
    "holo-homepage",
    "omnisearch",
    "unified-resolver",
    "scene-manifest",
    "self-verifying",
    "golden-ratio",
    "native-fidelity",
    "mobile-conformant",
    "web2-web3-ipfs-onion-ai"
  ],
  "note": "Verify-before-render of each page/component κ is proven by the κ-render spine (holo-render-registry-witness / holo-render-mount-witness). This witness proves the host wiring + content-addressed manifests statically; live κ-resolution runs through that spine.",
  "subFloorFonts": [],
  "checks": {
    "surface exists at usr/share/frame/homepage.html": true,
    "surface is registered in the ONE fhs route map (served flat at /homepage.html)": true,
    "the host carries NO inline page code — it streams a κ-page (thin host, <root> mount)": true,
    "mounts through the κ-render spine (holo-render), the verify-before-render path (Law L5)": true,
    "resolves a page by name (?p=, default \"home\") — one door, many pages": true,
    "a page manifest (ui/pages/pages.json) exists with a home page + breadth of doors": true,
    "EVERY page is named by a content address (did:holo:sha256 κ) — identity is content, not path": true,
    "the host loads the page manifest it resolves against (/ui/pages/pages.json)": true,
    "a κ-UI component registry (ui/vendor/registry.json) maps components to content κ": true,
    "a bare-import map (ui/vendor/bare.json) exists and the host binds holo:<name> → κ": true,
    "fidelity-adaptive: imports holo-fidelity and chooses a prefetch policy": true,
    "on a slow/save-data link, warms only first-paint criticals; eager-warms on capable devices": true,
    "defines the golden constant (φ = 1.618)": true,
    "mobile viewport (width=device-width, viewport-fit=cover)": true,
    "honors the device motion budget (reduced-motion guard wired)": true,
    "no hardcoded font-size below the 16px readability floor": true
  }
}
